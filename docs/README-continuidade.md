# Gestão IM360 — guia de continuidade

Leia este documento e o `CLAUDE.md` da raiz ao iniciar qualquer sessão. Depois, leia a página Notion **Decisões vigentes** (`3cd2f3f4-b9b2-8106-95cd-fc8d937bd953`) — em conflito, ela vence estes documentos.

## Contexto em uma frase

O sistema **Gestão IM360** (Flutter + Supabase + Cloudflare) substituirá a planilha `Gestão Interativo.xlsx` na gestão de alunos, turmas, vagas e material didático de uma escola com três métodos de ensino (Interativo, Inglês e Modular). Concepção e plano aprovados; a Fase 0 (Fundação) está em andamento, com target de go-live em outubro/2026.

## Documentos deste diretório

| Documento | Conteúdo | Estado |
|---|---|---|
| `plano-projeto-sistema.md` | Plano v1.0: visão, escopo, permissões, arquitetura, modelo de dados, regras, telas, migração, fases, riscos. As decisões do cap. 11 foram respondidas em 31/08/2026 — ver Decisões vigentes | referência |
| `analise-planilha-entendimento.md` | Entendimento funcional da planilha, 18 dúvidas respondidas, mapa técnico de colunas | concluído |
| `script-extracao-planilha.md` | Protótipo Python (openpyxl) de extração da planilha — base da ferramenta de migração da fase 8/9 do board | protótipo |
| `modelagem-dados-ddl.md` | DDL detalhado (33 tabelas, funções de infraestrutura, padrão de RLS) e mapa DDL → card das fases 3 a 8. **Fonte das migrações** | vigente |
| `identidade-visual.md` | Marca, paleta (com contrastes WCAG verificados), tipografia, badges de status e tokens Dart. **Fonte do design system (card 2.7)**. Arquivos em `assets/marca/` | vigente |
| `regra-virada-rep.md` | Card 2.5: critério objetivo da virada REP pontual → contínuo (débito × capacidade semanal × prazo), funções `fn_rep_situacao`/`fn_rep_avaliar_virada`/`fn_rep_virar_continuo`/`fn_rep_voltar_pontual`, pendências `REP_VIRADA` e os 8 ajustes que exige | vigente |
| `regras-negocio-funcoes.md` | Card 2.2: onde vive cada regra da seção 6 do plano (restrição, trigger, função de aplicação ou rotina `pg_cron`) e a assinatura de cada objeto; catálogo de erros, de pendências, ajustes que o DDL precisa receber e mapa função → card | vigente |
| `permissoes-matriz.md` | Card 2.4: catálogo de 49 permissões em 12 domínios, o que cada política de RLS exige tabela a tabela, matriz inicial dos 4 perfis, guardas das 13 rotas e o seed do card 3.6. **Fonte do catálogo de permissões** | vigente |
| `projecao-demanda.md` | Card 02.5 (Ordem 5): algoritmo da projeção de demanda de apostilas — cascata `MODULAR`/`RITMO_ALUNO`/`PREVISAO_CURSO`/`MEDIA_METODO`, `v_ritmo_aluno`, `v_projecao_aluno`, `rt_projecao_demanda`, snapshot mensal e critério objetivo de recalibração. **Fonte do algoritmo da projeção** | vigente |
| `views-leitura.md` | Card 2.3: SQL das views de leitura (estoque, demanda, pedido sugerido, vagas, dashboard, pendências), contrato da projeção, permissões de leitura por view e ajustes exigidos no DDL. **Fonte das views** | vigente |
| `wireframes.md` | Card 2.6: estrutura das 13 telas — shell responsivo, navegação (menu lateral / barra inferior do monitor), wireframe por tela com fontes de dados (views do 2.3), ações com permissões (2.4) e estados obrigatórios. **Fonte da estrutura das telas (entrada do card 2.7)** | vigente |
| `design-system.md` | Card 2.7: design system Flutter — tokens completos nos dois temas (badges do escuro definidos com contraste verificado), `ThemeData` Material 3, catálogo de componentes (`TabelaIm360`, badges, formulários, estados de tela), breakpoints como código e **textos finais de erro e estado vazio**. Apêndice com `lib/theme/` pronto para o card 3.7. **Fonte do design system aplicado** | vigente |
| `estrategia-testes.md` | Card 2.8 (+ 3.4.5): estratégia de testes (pgTAP + `supabase test db`), helpers que reproduzem o contexto do PostgREST — **implementados em `supabase/seed.sql`**, com o Apêndice A virando o registro do que mudou —, suíte de **catálogo** que transforma os ~35 ajustes acumulados dos cards 2.2–2.5 em asserção, obrigação de teste por tipo de card, mapa decisão → teste e **critérios de aceite dos 4 marcos**. **Fonte da estratégia de testes** | vigente |
| `acesso-autenticacao.md` | Card 3.5: fluxo de entrada no sistema — o invariante do espelho `auth.users` → `usuario`, a configuração do Auth (o que fica em `config.toml` e o que fica no painel), convite pelo painel na v1 e o contrato da Edge Function do card 4.7, login/sessão, recuperação de senha, desativar × banir × apagar, e a lista do que Irineu precisa configurar nos projetos remotos. **Fonte do fluxo de acesso** | vigente |
| `politica-credenciais-pcs.md` | Card 2.9: política das credenciais dos PCs do laboratório — segredo (usuário + senha) no Supabase Vault vinculado ao PC, permissão `salas.acessar_credencial` (direção e monitor), `fn_pc_credencial_ler`/`fn_pc_credencial_gravar`, log de acesso imutável como pré-condição da leitura, e rotação obrigatória na migração. **Fonte da política de credenciais** | vigente |
| `../app/README.md` | Card 3.7: como rodar e empacotar o app Flutter (os três `--dart-define`), o portão local (`dart format`/`analyze`/`test`) e o mapa de `lib/`. **Fonte do app** | vigente |
| `deploy-web.md` | Card 3.8: publicação web — o build (`--no-web-resources-cdn` e os três `--dart-define`), os dois projetos de Pages, por que **não** existe `_redirects`, o `_headers` com CSP e cache, a lista de conferência do painel do Supabase (Site URL, Redirect URLs com `/**`, SMTP) e o roteiro de verificação depois de publicar. **Fonte do deploy web** | vigente |
| `ci-cd.md` | Cards 3.9 (e 3.10, 3.11, 4.0,5): os **cinco** workflows (`testes`, `db-migrations`, `deploy-web`, `deploy-worker-vigia`, `backup-semanal`), o que dispara e o que cada um bloqueia, as versões fixadas e o motivo, a lista de serviços excluídos do stack de teste, os dois portões humanos da produção, **o que só Irineu configura** (secrets do Cloudflare e a chave publicável por environment) e como rodar o mesmo portão na máquina. §14 documenta o **portão de migrações** do card 4.0,5 — o que passa, o que reprova e por quê. **Fonte do pipeline** | vigente |
| `worker-vigia.md` | Card 3.10: o Worker Cloudflare que mantém os dois projetos Supabase acordados e avisa por e-mail quando algum não responde — a sonda e o que a resposta prova, o alerta, o que só Irineu configura, como rodar na máquina e os limites assumidos. **Fonte do vigia** | vigente |
| `backup-restauracao.md` | Card 3.11: backup semanal de produção (dump → **ensaio de restauração** → R2 com retenção de 12 cópias), por que a ordem dos três passos é essa, o procedimento de restauração — que é o mesmo script que o CI roda toda semana —, o que o dump **não** traz (painel do Auth, SMTP, Vault) e os limites assumidos, incluindo o modo de falha silencioso do próprio observador. **Fonte do backup** | vigente |
| `observabilidade.md` | Card 3.12: o Sentry no Flutter (DSN por `--dart-define`, sem DSN não inicializa), as peneiras que impedem PII de aluno de sair pelo breadcrumb e pelo `request`, o filtro do que merece virar evento, a CSP com asserção dos dois lados, os logs do Supabase e a vigilância da idade do backup no R2. **Fonte da observabilidade** | vigente |
| `seed-inicial.md` | Card 3.6: contrato de idempotência do seed (o que volta e o que não volta ao rodar de novo), tabela consolidada dos 15 parâmetros com a origem de cada valor, bootstrap do primeiro usuário de direção e a fonte única compartilhada com a escola-fixture. **Fonte do seed** | vigente |
| `administracao.md` | Cards 4.7 e 4.7.5: a tela de Administração (usuários com quem está sem perfil destacado, perfis e matriz, parâmetros, histórico), a **Edge Function do convite** (`supabase/functions/convidar-usuario`, o contrato do 3.5 §3.2 exercitado), o fechamento do convite que não terminava em lugar nenhum (`type=invite` lido antes do `Supabase.initialize`), os códigos do GoTrue traduzidos e o **histórico da matriz** (`perfil_permissao_hist` por trigger, imutável, e o seed que não devolve o removido). **Fonte da administração** | vigente |
| `emails-auth.md` | Card 4.7,6: os dois e-mails que o sistema manda — convite e recuperação de senha — em português e formatados. Por que **só existem dois** (e o motivo escrito dos outros quatro do GoTrue), a restrição de que o link é `{{ .ConfirmationURL }}` e não pode ser token (senão reabre o defeito do card 3.8), as decisões de desenho de HTML de e-mail (imagem bloqueada é o caso esperado, `alt` estilizado pelo `img`, botão em tabela, cores direto do guia de identidade), e **como isto chega a dev e prod** — o aplicador `supabase/templates/aplicar-templates.mjs`, com o aviso de nunca usar `supabase config push`. **Fonte dos e-mails do Auth** | vigente |
| `estimativa-entrega.md` | Card 3.13: método de estimativa da data de entrega — pontos (P/M/G/GG = 1/3/5/8) em vez de cards, calibração por **Tipo** e não por Fase, latência de terceiros separada do esforço, e a receita de 5 passos que os cards de recalibração das fases 04 a 09 executam. O resultado (P50/P80 e histórico) fica na subpágina Notion "Estimativa de entrega" do card 3.13. **Fonte do método de estimativa** | vigente |
| `cadeia-execucao.md` | Card 5.5,5: a cadeia de execução não interativa — uma sessão `claude -p` por card, em sequência, sem acompanhamento. O acordo `develop` é do CI / `main` é de Irineu, os seis pontos de parada, por que o driver **não acredita** no `CARD_OK` da sessão, o guarda de comandos destrutivos e o que a cadeia **não** faz. **Fonte da automação de execução** | vigente |

A planilha original (`Gestão Interativo.xlsx`, snapshot 29/08/2026) está no projeto do Claude.ai, não neste repositório.

## Marcos até aqui

- 29–30/08/2026 — análise da planilha, 18 pontos de requisitos validados, plano v1.0.
- 30/08/2026 — board no Notion (11 fases) e página Decisões vigentes criados; plano v1.1 (Word) enviado ao dono do produto.
- 31/08/2026 — dono do produto respondeu as 9 questões do cap. 11; decisões técnicas fechadas com Irineu; projetos Supabase dev/prod criados; repositório inicializado com este bootstrap. Planilha de conferência de alunos sem turma entregue ao pedagógico (20 alunos + 2 códigos divergentes).
- 31/08/2026 — board reconciliado com as decisões já tomadas e **card 2.1 concluído**: DDL detalhado em `modelagem-dados-ddl.md`. Card 2.5 criado para o critério objetivo da virada REP pontual → contínuo.
- 01/09/2026 — **card 2.5 concluído**: critério objetivo da virada REP fechado em `regra-virada-rep.md`. Virada **sugerida** (pendência `REP_VIRADA`), nunca automática — virar contínuo cria alocação permanente e consome vaga toda semana. Prazo de 30 dias corridos por aula perdida, com quatro parâmetros novos (`rep_prazo_dias`, `rep_capacidade_semanal`, `rep_faltas_max`, `rep_janela_volta_dias`). Destrava as regras de admissão e lotação da Fase 5.
- 01/09/2026 — promoção `develop` → `main` (PR #7): as duas branches voltaram a ter conteúdo idêntico, sem nenhuma migração envolvida.
- 31/08/2026 — **card 1.9 concluído**: identidade visual fechada em `identidade-visual.md` + SVGs em `assets/marca/`. Paleta inspirada no Instituto Mix sem copiar (o sistema é de um franqueado, não é produto da franqueadora): laranja de marca, estrutura em grafite-azulado, vermelho reservado a erro. Destrava os cards 2.6 (wireframes) e 2.7 (design system).
- 01/09/2026 — **card 2.4 concluído**: catálogo de permissões e matriz inicial em `docs/permissoes-matriz.md`. 49 códigos `<dominio>.<acao>` em 12 domínios (15 do card 2.2, 9 do card 2.3, 25 novos); domínio no **plural**, fechando o ajuste #7 do card 2.3. Achado central: **várias escritas acontecem como efeito colateral dentro da transação de outro ator** — as funções do card 2.2 são `security invoker`, então a entrega do monitor escreve em `aluno_material`, `aluno_material_hist`, `certificado_checklist` e `pendencia`, e o padrão de quatro políticas do card 2.1 barraria todas. Nove tabelas ganham política fora do padrão, `movimento_estoque` ganha insert **por tipo** (senão o monitor lança ENTRADA pelo PostgREST) e `pendencia` fica sem exigência de domínio no insert. Doze achados no total, seis bloqueantes.
- 01/09/2026 — **card 2.3 concluído**: views de leitura especificadas em `docs/views-leitura.md`. Decisões estruturais: `security_invoker = on` em toda view, nenhuma `materialized view` (matview não respeita RLS), `fn_hoje()` no fuso de São Paulo em lugar de `current_date`, e `qtd_projetada` reservada como `0` em `v_pedido_sugerido` para a Fase 8 entrar por `create or replace`. Oito ajustes registrados para os cards de migração, dois deles bloqueantes.
- 01/09/2026 — **card 2.6 concluído**: wireframes das 13 telas em `docs/wireframes.md`. Estrutura, navegação e estados; desktop-first para secretaria, barra inferior mobile centrada nas três jornadas do monitor (registrar entrega, financeiro OK, manutenção de PC). Decisões transversais: botão sem permissão é ocultado (sem estado, desabilitado com motivo); a tela nunca pré-verifica regra em Dart — chama a função e reage aos status de retorno; central de pendências com ação contextual por tipo (`REP_VIRADA` executada de lá). Destrava o card 2.7 (design system).
- 01/09/2026 — **card 2.7 concluído**: design system Flutter em `docs/design-system.md`, fechando a cadeia identidade (1.9) → estrutura (2.6) → aplicação (2.7). Badges no tema escuro definidos (a lacuna do 1.9) com todos os pares AA verificados; `ColorScheme` montado à mão (nunca `fromSeed`); sistema plano com bordas; textos finais de erro (por `codigo`) e de estado vazio fechados para todas as telas. Destrava o card 3.7 (esqueleto Flutter, que copia `lib/theme/` do apêndice).
- 01/09/2026 — **card 02.5 (Ordem 5) concluído**: algoritmo da projeção de demanda especificado em `docs/projecao-demanda.md`, preenchendo o contrato que o card 2.3 deixou reservado. Cascata escolhida **por aluno**, nunca por item; a projeção começa no **segundo** item pendente da trilha (a parcela imediata é a primeira), sem o que todo aluno pesaria duas vezes no pedido sugerido; ritmo individual por média dos três intervalos mais recentes com piso de 7 e teto de 120 dias; previsão de conclusão vencida não serve de base; grão mensal mantido. Nove parâmetros novos, snapshot mensal (`demanda_projetada_hist`) para tornar a recalibração do card 11.2 uma medição, e o achado #4 do card 2.4 (`fn_param_int` como `security definer`) escalado a **bloqueante**.

- 01/09/2026 — **card 2.8 concluído**: estratégia de testes e critérios de aceite em `docs/estrategia-testes.md`. Escolhido **pgTAP** com `supabase test db` (o projeto testa muito mais estrutura e exceção do que resultado de `select`). Dois achados estruturais: (1) a maior parte dos ~35 "ajustes que o DDL precisa receber" acumulados nos cards 2.2–2.5 e Ordem 5 é da mesma família — valor gravado que o `check` recusa, ou permissão exigida que o seed não cria — e **falha em silêncio dentro do `exception` da rotina diária**, mas é toda verificável por **teste de catálogo** de dezenas de linhas; (2) teste de view não é "não dá erro", é **paridade de linhas entre perfis autorizados**, porque a RLS reduz em silêncio e a tela vazia mente. Sem meta percentual de cobertura: o portão é obrigação por tipo de card. Marco 3 teve divergência registrada — a planilha **não projeta demanda**, então metade do marco não é comparável e passa a ter critério próprio (reprodução por degrau, disjunção e banda de plausibilidade). Card **3.4.5** criado para os helpers e a escola-fixture.
- 01/09/2026 — **card 2.9 concluído**, encerrando a Fase 2: política de credenciais dos PCs em `docs/politica-credenciais-pcs.md`. Irineu decidiu **campo cifrado no Supabase Vault vinculado ao PC**, com acesso **só de monitor e gerência** — a recomendação original do plano (não armazenar) se inverteu quando ficou claro que a credencial é o **login da máquina** e quem a consulta é o **monitor, no laboratório, com frequência**: um cofre externo não seria barreira, seria desvio, e a senha voltaria para o papel ou para a planilha. O par usuário+senha inteiro vira um segredo `jsonb` no Vault (nada em claro, o que evita proteção por coluna, já rejeitada no card 2.4); `pc` guarda só o ponteiro e o carimbo. Uma permissão nova (`salas.acessar_credencial`, 49 → 50 códigos), um código de erro novo (`PC_INEXISTENTE`, fixture de 21 → 22) e log de acesso **imutável e sem `exception`** — falha ao registrar derruba a leitura. As senhas atuais são consideradas queimadas: a migração não traz senha e a planilha congelada é sanitizada.

- 01/09/2026 — **card 3.3 concluído**: primeira migração de verdade, `supabase/migrations/20260901140000_organizacao_acesso.sql` — `fn_auditoria`, as sete tabelas de organização e acesso, os sete triggers de auditoria e **RLS habilitada e forçada nas sete, sem política nenhuma** (as políticas são do card 3.4). Falhar fechado é obrigatório porque "Automatically expose new tables" está ligado: tabela criada sem RLS é uma API aberta durante toda a janela entre este merge e o do 3.4. Nasceram também `supabase/tests/010_catalogo_rls.sql` e `011_catalogo_convencoes.sql` (C1–C4, C6–C9 do card 2.8) e o bootstrap de `supabase/seed.sql` (pgTAP + schema `tests`), sem o qual a suíte que o card 2.8 declara obrigatória "desde a primeira migração" não rodaria até o card 3.4.5. Dois achados de fato: **o papel `postgres` do Supabase TEM `BYPASSRLS`** (e `service_role` também), o contrário do que as Decisões vigentes registravam — o seed do card 3.6 escreve sem problema, mas toda função `security definer` de propriedade do `postgres` ignora a RLS por inteiro, o que promove o teste C8 a barreira de segurança; e `unidade` ganhou `codigo` (chave natural estável), porque `nome` é editável na tela de Administração e não serve de chave de idempotência para o seed.

- 01/09/2026 — **card 3.4.5 concluído**: infraestrutura de teste em `supabase/seed.sql` — helpers `tests.*` (agora nove: `criar_usuario`, `uid`, `unidade`, `autenticar`, `como_anonimo`, `como_rotina`, `encerrar_sessao`, `conta_como`, `codigo_do_erro`) e a **camada `acesso`** da escola-fixture (duas unidades, cinco perfis, catálogo e matriz das sete permissões que as políticas citam, oito usuários). Nasceu `supabase/tests/001_infra_teste.sql` e o `020` foi reescrito para consumir a fixture — ele criava a própria `ESCOLA_A` e, com o seed no lugar, `unidade_codigo_uk` o derrubaria. Três achados: (1) o **Apêndice A do card 2.8 tinha um erro de execução** — `tests.conta_como` terminava chamando `tests.encerrar_sessao()`, e nessa altura a sessão já está em `authenticated`, que não tem `USAGE` no schema `tests`: a paridade do §6.3 morreria com `permission denied` na primeira chamada; (2) `tests.criar_usuario` não era idempotente (`gen_random_uuid()` a cada chamada contra `usuario.email unique`), e o `on conflict (id) do update` que resolve isso também é o que faz o helper sobreviver ao trigger de espelhamento do card 3.5; (3) **quatro dos seis elementos da fixture moram em tabelas que ainda não existem** (materiais, alunos, salas/PCs, blocos — cards 4.1 a 6.1), então em vez de escrevê-los às cegas ou comentá-los, cada camada ficou **declarada** em `tests.fixture_camada` com a condição que a torna devida, e `001_infra_teste` reprova a suíte no dia em que a tabela nascer sem a camada junto — com asserção de que o portão de fato reprova. ✅ **A suíte rodou com `supabase start` + `supabase test db`, pgTAP 1.3.2 de verdade: 73 asserções, 73 verdes** (5+7+32+29), fechando a dúvida que o card 3.4 deixou aberta — o shim daquela sessão aceitava as mesmas chamadas que o pgTAP real aceita.

- 01/09/2026 — **card 3.4 concluído**: `supabase/migrations/20260901163000_acesso_rls.sql` fecha a janela aberta pelo 3.3 — `fn_hoje`, `fn_contexto_rotina`, `fn_unidade_atual`, `tem_permissao`, `fn_minhas_permissoes`, `fn_exige_permissao`, `fn_param_txt`/`fn_param_int` e as **19 políticas** das sete tabelas, exatamente como a matriz do card 2.4 §4 as descreve. Três achados: (1) **a matriz do 2.4 não deixa ninguém montar a sessão do app** — ler a própria linha de `usuario` e a própria lista de permissões exige `admin.ler`, que só a direção tem, então monitor, secretaria e pedagógico entrariam sem saber o que podem fazer; resolvido com `or id = auth.uid()` na política de `select` de `usuario` e com `fn_minhas_permissoes()`, que devolve as permissões **do próprio usuário** (nada que ele já não descubra chamando `tem_permissao` código a código); (2) `tem_permissao` ganhou três filtros que o DDL do 2.1 não tinha — `usuario.ativo`, `perfil.ativo` e perfil da mesma unidade —, sem os quais **desativar um perfil na tela de Administração seria uma ação sem efeito nenhum**; (3) `unidade` é a única tabela cujo `insert` não pode filtrar unidade, porque unidade nova por definição não é a unidade corrente de ninguém — o controle ali é `unidades.gerir`. Nasceu `supabase/tests/020_acesso_tem_permissao.sql` (32 asserções) — e a suíte inteira **rodou pela primeira vez, 44/44 verdes**, num Postgres em container (ver "Próximos passos", item 5) e a suíte de catálogo ganhou a asserção **(tabela, comando)** simétrica, um C11 parcial sobre os códigos citados nas políticas e a **premissa `BYPASSRLS` virada em portão** — sem ela `tem_permissao` recursa na política de `usuario_perfil` e o sistema inteiro para na primeira consulta.

- 01/09/2026 — **card 3.5 concluído**: `supabase/migrations/20260901180000_auth_espelhamento.sql` e `supabase/config.toml` — o espelho `auth.users` → `usuario` como trigger no banco, a configuração do Auth versionada e `docs/acesso-autenticacao.md`. O invariante: quem entra em `auth.users` ganha linha em `usuario` no mesmo instante, e `usuario.email` é **sempre** o do Auth. As duas metades protegem o mesmo silêncio — usuário autenticado sem espelho não é usuário sem acesso, é usuário que vê **todas as telas vazias, sem erro nenhum**; e e-mail divergente é uma tela mostrando um endereço com o qual ninguém entra. Três achados de execução, os três medidos com o stack local no ar: (1) **`[auth.email] enable_signup = false` não desliga o cadastro público, desliga o provedor de e-mail inteiro** — com ela em `false` o convite ainda sai, mas login por senha e recuperação respondem `400 email_provider_disabled`, e a mensagem não fala de cadastro; quem fecha o cadastro é o `enable_signup` de `[auth]`, e ele sozinho basta; (2) **a fixture do card 3.4.5 não logava** — sem `instance_id` a Admin API responde "user not found" e com os campos de token em `null` o login morre em `converting NULL to string is unsupported`, um 500 sem relação aparente com a causa; os oito usuários agora entram de verdade no stack local, que é o que o card 3.7 precisa; (3) **a convenção `PT<status>` do card 2.2 foi exercitada pelo PostgREST pela primeira vez** e funciona: o `PATCH` do e-mail devolveu **HTTP 409** com o `codigo` `EMAIL_IMUTAVEL` dentro de `details`. A unidade do convite vem do metadado ou da **única unidade ativa** — um default que se fecha sozinho quando a segunda unidade nascer (Fase 11). Três códigos de erro novos (fixture de contrato 22 → 25) e sete ajustes, dois bloqueantes (Site URL do card 3.8; perfil do primeiro usuário no seed do 3.6). Suíte: **91 asserções, 91 verdes** (73 + as 18 de `021_auth_espelhamento`), mais o convite, o login, a recuperação e o `signup` recusado exercitados pela API de verdade.
- 01/09/2026 — **card 3.6 concluído**, encerrando o seed de acesso: `supabase/migrations/20260901200000_seed_inicial_acesso.sql` cria a unidade real (`MATRIZ` — Instituto Mix Charqueadas), as **50 permissões**, os quatro perfis, a matriz do card 2.4 §5 (direção 50, secretaria 37, pedagógico 22, monitor 14), os **15 parâmetros** que as regras já escritas leem e o **bootstrap do primeiro usuário de direção**. Documentado em `docs/seed-inicial.md`; suíte nova `supabase/tests/022_seed_inicial.sql`. **A suíte inteira roda verde com `supabase start` + `supabase test db`: 115 asserções, 115 verdes**, com as quatro migrações e o `seed.sql` aplicando limpo num banco vazio — e o fluxo foi exercitado pela API de verdade (convite pela Admin API, login por senha, `rpc/fn_minhas_permissoes` devolvendo 50 códigos). Três achados: (1) **o contrato de idempotência do card 2.4 §8 se contradizia** — "não desfaz configuração" e "acrescenta o que faltar" são a mesma frase, e o seed devolvia no deploy seguinte a permissão que a direção tinha DESMARCADO na tela, sem erro e sem log; o guarda passou a ser **por código** (só distribui código que ainda não tem linha nenhuma na unidade), o que mantém código novo chegando e deixa um caso residual assumido — código tirado de *todos* os perfis volta, e quem o fecha é o card **4.7.5**; (2) o bootstrap da direção precisa de **duas metades** (a chamada no fim da migração, para o convite que já aconteceu, e um trigger `after insert on usuario`, para o convite que vem depois do deploy): só a primeira rodaria em produção, não encontraria ninguém e não faria nada; (3) a **"política de entrega sem estoque" não vira parâmetro** — ela é algoritmo de `fn_registrar_entrega` com três status de retorno, e um parâmetro sem leitor seria decoração. A escola-fixture perdeu o catálogo próprio: as duas unidades agora chamam `public.fn_seed_acesso()`, a mesma função da migração (camada `acesso_seed_real` do card 3.4.5), e o teste assere que os conjuntos de códigos são idênticos. C11 passou à versão **cheia**: todo código citado em política existe no catálogo gravado.

- 02/09/2026 — **os dois ambientes entraram no ar**, fechando o que o card 3.8 tinha deixado com Irineu: Pages publicado nos dois projetos, `app.gestaoim360.com` e `homolog.gestaoim360.com` anexados, painel do Auth preenchido e **SMTP próprio** (Resend, `gestaoim360.com` verificado). Três achados de execução, todos medidos: (1) **o custom domain da homologação não sobe sozinho junto com o deploy** — o bundle novo já montava o link de recuperação para `homolog.gestaoim360.com` enquanto o domínio ainda era NXDOMAIN, e o login por senha seguia funcionando, então nada na tela denunciava; só a consulta de DNS mostrou; (2) **o free tier do Resend aceita 3 domínios, não 1** — a conta que já servia o Entrelares comportou `gestaoim360.com` sem conta nova nem conector novo; (3) o SPF e o MX ficam em `send.gestaoim360.com` e o DKIM na raiz, então um e-mail próprio no domínio (Google Workspace, por exemplo) convive sem conflito. Verificação que vale registrar como método: o `redirect_to` do e-mail entregue foi **seguido** nos dois ambientes e o `Location` caiu em `/redefinir-senha` — é a única prova de que a lista de Redirect URLs está certa, porque lista errada não dá erro, só devolve a Site URL.
- 01/09/2026 — **card 3.8 concluído**: contrato de publicação web em `docs/deploy-web.md`, `app/web/_headers` e a marca em contornos. Três achados, os três medidos com o build de release servido e o stack local no ar: (1) **o fragmento da URL é do Auth, não do roteador** — o app vinha do card 3.7 com a rota no fragmento, e todo link que o Supabase gera fora do fluxo PKCE (o **convite pelo painel**, que é como a v1 cria usuário) volta como `#access_token=…&sb=…`; o `supabase_flutter` cria a sessão, limpa o que conhece e sobra `#sb`, que o `go_router` lê como destino: **"Esta tela não existe. Código: /sb", já autenticado**. Corrigido com `usePathUrlStrategy()`, e a contrapartida (servidor devolver `index.html` em qualquer caminho) o Pages já faz de graça; (2) **o build de release busca o CanvasKit no `gstatic.com`** e, com o CDN inalcançável, o app abre em **tela branca sem erro nenhum** — `--no-web-resources-cdn` passa a ser obrigatório; (3) a lista de **Redirect URLs casa por igualdade, não por prefixo**, e destino recusado **não dá erro**: cai na Site URL, e o link de recuperação leva a pessoa para a tela inicial em vez da de nova senha. A regra de SPA que se copia da internet (`/*  /index.html  200`) **quebraria o app no Pages** — lá "os redirects são sempre seguidos, exista ou não um asset" —, então não há `_redirects` e as duas armadilhas (essa e um `web/404.html`) viraram asserção em `app/test/publicacao_web_test.dart`. Fechada a pendência de `text-to-path` do card 1.9: os três SVGs da marca são só geometria, e o favicon e os quatro ícones do PWA saíram do símbolo — eram os do `flutter create` até aqui. **98 testes Flutter verdes** (94 + 4), `analyze --fatal-infos` e `dart format` limpos, suíte do banco **115/115** (este card não tem migração). Falta o que só Irineu faz: criar os dois projetos de Pages e configurar Site URL, Redirect URLs e SMTP nos dois projetos Supabase.
- 01/09/2026 — **card 3.7 concluído**: o app existe. `app/` com Flutter 3.47.2 (web + Android + iOS, app id **`com.gestaoim360.app`**), `go_router`, Riverpod, `supabase_flutter`; `lib/theme/` copiado do apêndice do card 2.7, Inter empacotada em quatro pesos, shell responsivo nas três faixas, tela de login com recuperação de senha e as **13 rotas guardadas pelo conjunto mínimo do card 2.4 §6**. O contrato `test/fixtures/codigos_erro.txt` nasceu na **raiz** (25 códigos) com o catálogo Dart do outro lado. Quatro achados: (1) **"autenticado sem espelho" e "usuário desativado" são indistinguíveis pelo app** — `fn_unidade_atual` e `tem_permissao` exigem `usuario.ativo`, então o desativado também lê zero linhas da própria linha de `usuario`, e o texto da tela teve de cobrir os dois; (2) **"entrou e não tem perfil" precisava de estado próprio** — com `fn_minhas_permissoes()` vazio o shell abriria sem nenhum item de menu, o mesmo silêncio que o card 3.5 combateu; (3) os **SVGs da marca usam `<text>`**, que o `flutter_svg` não renderiza — a pendência de `text-to-path` do card 1.9 deixa de ser só de uso externo e alcança o ícone do app (3.8/10.1); a marca foi desenhada em Flutter enquanto isso; (4) o **Dashboard exige cinco permissões**, então um perfil enxuto entraria e cairia numa tela sem acesso logo depois de digitar a senha certa — o roteador leva à primeira rota que a pessoa abre. **94 testes verdes**, `flutter analyze --fatal-infos` limpo, `dart format` limpo e build web de release compilando.
- 02/09/2026 — **card 3.10 concluído**: o **vigia** em `worker-vigia/`, Worker Cloudflare com cron diário (`0 9 * * *` = 06:00 em São Paulo) que sonda os **dois** projetos Supabase e manda e-mail pelo Resend quando algum não responde, documentado em `docs/worker-vigia.md`. Projeto do plano gratuito pausa com 7 dias sem atividade, e **projeto pausado é o app fora do ar** — a mitigação e a verificação da mitigação são a mesma requisição: se o `select` diário não estiver evitando a pausa, quem descobre é o próprio vigia. A sonda é um `select` anônimo em `parametro`, e **a resposta certa é `[]`** (o `anon` não satisfaz política nenhuma, card 3.4) — o que ela prova é que o PostgREST executou consulta num Postgres vivo; a asserção é **positiva** (200 *e* lista JSON), porque "não deu erro" passaria numa página de manutenção servida com 200. Três achados de execução: (1) **o workerd recusa o Worker inteiro se o módulo de entrada exportar uma constante** (`Incorrect type for map entry 'CAMINHO_SONDA'`) — o vigia simplesmente não existiria, e o sintoma seria a *ausência de e-mail*, que é o que se espera quando está tudo bem; daí `src/vigia.js` + `src/index.js`, com asserção que reprova a volta; (2) sair do Node no meio do desmonte do `fetch` derruba o processo no Windows com código 127 que não é o da sonda; (3) produção respondeu `401` em ~300 ms à rodada com chave errada de propósito, o que prova o endereço no ar sem usar credencial nenhuma. Quarto workflow, `deploy-worker-vigia.yml`, publicando só de `main` com o portão do environment `prod`, e job novo `vigia` no `testes.yml` (`node --test`, **sem `npm install`**: o Worker não tem dependência). ✅ **Exercitado, não lido**: suíte **12/12**, `wrangler dev` rodando as duas sondas de verdade no workerd (dev 200/0 linhas; produção 401 com chave errada) e o `scheduled` falhando alto com o nome do segredo que faltava. ✅ **O vigia entrou no ar no mesmo dia**: Irineu ampliou o token e criou os três secrets, o PR #46 promoveu a `main` e o `deploy-worker-vigia` ficou **verde na estreia** — job parado em `waiting` até o *Approve and deploy*, de modo que o **portão duplo da produção passou a valer também para a infraestrutura**. `gestao-im360-vigia` publicado, `schedule: 0 9 * * *` registrado, três secrets instalados, *startup* de 4 ms. E o passo de conferência mediu o que a sessão não tinha conseguido: **a sonda de produção respondeu `200`** (1371 ms; homologação 951 ms), as duas com a chave certa e **antes** de as chaves entrarem no Worker. Primeira execução agendada: 03/09/2026 às 06:00.

- 02/09/2026 — **card 3.11 concluído**: **backup semanal de produção** em `.github/workflows/backup-semanal.yml`, com os dois scripts de `backup/` e `docs/backup-restauracao.md`. Todo domingo às 06:30 de São Paulo: dump de produção (três arquivos), **restauração num banco vazio com conferência** e só então publicação no R2, mantendo as 12 cópias mais recentes. Três decisões estruturam o card. (1) **A ordem é dump → ensaio → publicação**, e não dump → publicação: publicar antes de conferir faria um dump quebrado ocupar uma das 12 vagas da retenção e, semana após semana, **empurrar para fora as cópias boas** — o backup se destruiria sozinho, em silêncio. (2) **O ensaio converte "backup nunca restaurado não é backup" (card 2.8 §15) de ritual anual em asserção semanal**, com asserções **positivas** pela mesma razão que o vigia não se contenta com "não deu erro": a comparação de tabelas é **simétrica** contra as migrações de `main` (falta = schema truncado; sobra = SQL aplicado à mão em produção) e as cinco tabelas de configuração precisam ter linha — dump de schema sem dado é o jeito mais comum de um backup ser inútil, e `pg_dump` sai com 0 nele. (3) **Este workflow não passa pelo environment `prod`, de propósito**: o *required reviewer* é o portão do que **muda** produção, e backup só **lê** — atrás do portão ele ficaria em `waiting` todo domingo esperando um clique que ninguém dá no fim de semana, e backup que espera aprovação é backup que não acontece. Registrada a divergência com a nota do card (`supabase db dump` em vez de `pg_dump` direto: o runner traz cliente 16 contra servidor 17, e o card 3.9 fixou uma ferramenta só) e o duplo papel que o card 3.10 pediu — **falha deste workflow lê-se "o backup não saiu **e** produção pode ter pausado"**. ⚠️ Modo de falha do próprio observador, escrito no §7: o GitHub **desativa workflow agendado em repositório com 60 dias sem commit**, e o risco vira real justamente quando o desenvolvimento parar. ⚠️ Falta o que é de Irineu: criar o bucket R2 `gestao-im360-backup` e os dois secrets `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY` — sem eles o workflow reprova no primeiro passo, nomeando o que criar.

- 02/09/2026 — **card 3.12 concluído, encerrando a Fase 3**: observabilidade em `docs/observabilidade.md` — **fonte da observabilidade**. O Sentry entra no app por `--dart-define=SENTRY_DSN` e **sem DSN não inicializa** (a suíte e o `flutter run` não falam com terceiro nenhum); projeto `irineu-pinheiro/gestao-im360`, um só para os dois ambientes, separados por `environment`. O trabalho de decisão não foi ligar o SDK — foi **impedi-lo de vazar dado de aluno**, porque o caminho natural vaza sem avisar: no PostgREST **o filtro vai na query string** (`?nome=ilike.*Maria*`, `?codigo_sgf=eq.3527`), e o breadcrumb de HTTP levaria o nome junto; `enableUserInteractionBreadcrumbs` (default `true`) registra o **rótulo do widget tocado**, que aqui é o nome de um aluno. As peneiras são funções puras e por isso testáveis sem rede. Duas armadilhas medidas: **o `copyWith` do SDK não serve para limpar** (usa `??`, então `null` mantém o valor antigo — uma limpeza que não limpa, em silêncio), e **`SentryUser` tem um `assert`** que derrubaria o próprio `beforeSend` ao reduzir ao `id` quando não há `id`. O filtro do que vira evento é `!traduzido && original is! AuthException`: os 25 códigos do catálogo são regra de negócio (turma cheia é a turma enchendo) e senha errada é o evento mais frequente que existe — mandar os dois esgotaria a cota do free tier com o que menos precisa de investigação. O **gancho mora em `traduzirErro`, não nas cinco telas**, para que toda tela da Fase 4 entre coberta sem ninguém lembrar do Sentry. Fechado o bloqueante herdado do 3.8/3.9: `https://*.ingest.us.sentry.io` no `connect-src` **no mesmo commit**, com asserção dos **dois** lados — o teste Dart exige que a CSP cite a ingestão, e um passo novo do `deploy-web` extrai o host do `SENTRY_DSN` de verdade e reprova quando a CSP não o cobre (exercitado com quatro DSNs sintéticos e com o real). **Achado que custou uma verificação:** derivar o rótulo do ambiente do `SUPABASE_URL` embutia os dois refs de projeto em **todo** bundle e **quebrava a contraprova do card 3.9** (o bundle de homologação não pode conter a URL de produção) — a derivação foi desfeita em favor de `--dart-define=APP_AMBIENTE`, e a contraprova virou passo obrigatório do workflow em vez de conferência manual. O vigia (card 3.10) passou a **conferir a idade do backup no R2**, fechando o modo de falha silencioso que o card 3.11 registrou (o GitHub desativa workflow agendado em repositório com 60 dias sem commit): limite de 9 dias, asserção positiva (`data.sql.gz` + `MANIFESTO.txt`), idade lida do **prefixo de data** e não do `uploaded`, e falha que alerta sem cegar as sondas do Supabase. ✅ **Exercitado, não lido**: **117 testes Flutter verdes** (98 + 19), `analyze --fatal-infos` e `dart format` limpos com Flutter 3.47.2, build de release com os quatro `--dart-define` conferido no `main.dart.js`, **25 testes no vigia** (12 + 13) e a vigilância do backup rodada **no workerd de verdade** com `wrangler dev` (bucket vazio reprova; cópia de 2026-08-31 passa com `idadeDias: 2`), o que confirma que a API real do `env.BACKUP.list()` bate com a suíte. ⚠️ **Falta o que é de Irineu, e sem isso nenhum evento chega ao Sentry**: cadastrar `SENTRY_DSN` como secret do repositório — é o **único** secret opcional do projeto, e o `deploy-web` avisa em vez de reprovar, de propósito. Roteiro de conferência de estreia em `docs/observabilidade.md` §11.

- 02/09/2026 — **card 3.9,5 concluído: o deploy do CI não chegava aos endereços públicos.** Achado na sessão do card 3.12 e medido de novo antes de corrigir: depois de um `deploy-web` **verde** em `develop`, `homolog.gestaoim360.com` servia `95065faf…` enquanto o CI acabava de publicar `ac1c466c…` (em `develop.gestao-im360-homolog.pages.dev`); e `app.gestaoim360.com` (`7311a8da…`) ≠ `main.gestao-im360.pages.dev`. Os dois endereços públicos serviam, desde o card 3.9, os *direct uploads* feitos à mão no card 3.8. **Causa:** os dois projetos de Pages nasceram por *direct upload*, e num projeto assim a *production branch* fica com um valor que o CI nunca usa — `wrangler pages deploy --branch <x>` só publica em produção quando `<x>` bate com ela, e como não bate **todo deploy do CI vira preview**: ganha URL própria, imprime `Deployment complete`, o workflow fica verde e o site continua na versão anterior, **que funciona** — o que remove o último sinal que restaria. A conferência do card 3.9 mediu a coisa certa nas URLs erradas, e o roteiro a olho do `docs/deploy-web.md` §10 não denunciaria: as duas telas são idênticas. **A correção tem duas metades e a segunda é a que dura.** Feita aqui a segunda: passo novo no `deploy-web.yml` que, **depois** de publicar, baixa `$APP_URL_BASE/main.dart.js` e exige que o sha256 seja o do `build/web/main.dart.js` recém-construído — seis tentativas espaçadas de 10 s (propagação não é instantânea, e asserção que reprova por pressa ensina a ignorar vermelho), cache-buster na query e `curl --compressed` (o Cloudflare comprime; comparar bytes comprimidos daria diferença sempre). Só corre em `main` e `develop` — dispatch de outra branch é preview de propósito. ✅ **Exercitada, não lida**: o script foi extraído do YAML e rodado contra os endereços de verdade nos três estados — aprova no endereço que tem o bundle (1ª tentativa), reprova no endereço público com o defeito (`dea0ec22…` × `d2b9569b…`) e reprova com "não respondeu" num host que não resolve; sem o primeiro caso a asserção poderia estar reprovando tudo. ⚠️ **Falta a metade que é de Irineu, e até lá o `deploy-web` fica VERMELHO nas duas branches, de propósito**: painel do Cloudflare → Workers & Pages → cada projeto → Settings → Builds & deployments → **Production branch** = `develop` no `gestao-im360-homolog` e `main` no `gestao-im360`; depois, um `workflow_dispatch` do `deploy-web` em cada branch. Vermelho que diz a verdade é melhor que verde que mente, e o que está no ar continua no ar — a asserção não desfaz publicação nenhuma. Detalhe em `docs/deploy-web.md` §5.9 e `docs/ci-cd.md` §7.

- 02/09/2026 — **card 4.2 concluído: o aluno existe, e mudar o status dele é regra do banco.** `supabase/migrations/20260902210000_alunos_status.sql` cria `aluno` e `aluno_status_hist` (RLS habilitada, **forçada** e com as cinco políticas do card 2.4 §4 — `aluno` sem `delete`, histórico sem `update` nem `delete`), a tabela de decisão `fn_aluno_transicao_valida`, o gate de FORMADO, os dois triggers de status e as funções `fn_aluno_alterar_status`/`fn_aluno_reverter_status`. Nasceu `supabase/tests/030_alunos_status.sql` (56 asserções) e a camada `alunos` da escola-fixture (12 alunos por unidade, um por caso que alguma decisão criou). **Estrutura e mais nada:** nenhum aluno em `migrations/`, e o portão do card 4.0,5 confirma — "nenhuma escrita de dado". Cinco achados. (1) **A política de update de `aluno` deixava mudar status a quem só tem `alunos.editar`**: o quadro do card 2.4 dá uma política para a tabela inteira com o `or` de três códigos, e **RLS não é por coluna** — um perfil montado na tela do card 4.7 com `alunos.editar` e sem `alunos.alterar_status` faria `PATCH status` pelo PostgREST. Fechado com a permissão reconferida **dentro do trigger**, que é o mesmo desenho que o card 2.4 deu ao `certificado_checklist`; visto vermelho antes de ser aceito (sem o guarda, o update passa **sem exceção nenhuma**). (2) **A GUC do motivo é da TRANSAÇÃO, não da chamada**: sem limpá-la no fim de `fn_aluno_alterar_status`, a segunda mudança de status da mesma transação herda o motivo da primeira — medido na bancada, o histórico saiu com `"trancou para trabalhar"` numa transição que não tinha motivo nenhum. (3) **Os dois triggers precisam de `when (old.status is distinct from new.status)`**: sem ele, um `PATCH` que reenvia a linha inteira com o mesmo status responde `TRANSICAO_INVALIDA` — corrigir a observação de um aluno cancelado viraria erro. (4) **O gate de FORMADO só tem metade** — a condição do certificado ENTREGUE depende de `certificado_checklist` (card 8.3) —, e o portão que a cobra **precisou remover os comentários do corpo antes de procurar a citação**: `prosrc` inclui o comentário que descreve o que falta, e o portão teria aprovado a si mesmo no dia em que a tabela nascesse. (5) `aluno_status_hist` é escrita por trigger `invoker`, então a política de insert tem de aceitar `POST` direto — daí `tg_aluno_status_hist_coerente`, que exige que a linha descreva o estado real do aluno: **histórico que mente é pior que histórico ausente, porque tem cara de prova**. Um código de erro novo, `ALUNO_INEXISTENTE` (contrato de **25 → 26**), pelo precedente do `PC_INEXISTENTE`: `ALUNO_INATIVO` diz "só vale para ATIVO ou ACELERAR" e seria mensagem errada. Três triggers do card 2.2 §3.2 ficam para as fases seguintes (`tg_aluno_status_desaloca` 5.1, `tg_aluno_trilha_inicial` 6.2, `tg_aluno_combo_alterado` 5.5), cada um com portão no teste 030 que reprova no dia em que a tabela dele nascer. ✅ **Exercitado, não lido**: `supabase start` do zero com as **seis** migrações e o `seed.sql` limpos, suíte **194/194 `Result: PASS`** (eram 138), portão de migrações verde e **reprovando na bancada** a carga de alunos escondida dentro de função, e cada asserção nova conferida **vermelha** contra a migração quebrada de propósito. ⚠️ O lado Flutter (uma entrada no catálogo de erros) **não foi exercitado nesta sessão**: a imagem traz Flutter **3.35.0** e o `pubspec` exige Dart `^3.13.2` — `flutter test` nem resolve dependências. O portão é o job `app` do CI, que roda a versão fixada 3.47.2; a simetria dos dois lados do contrato (26 códigos) foi conferida à parte.

- 02/09/2026 — **card 4.0,5 concluído: nenhuma migração grava dado de negócio.** Job novo `migrações (portão)` no `testes.yml`, com o varredor em `portao-migracoes/varredor.mjs`. Nasce da decisão do mesmo dia — dado da planilha fica restrito a dev/homolog até a virada do card 9.7 — e existe porque **regra escrita não basta**: todo `.sql` em `supabase/migrations/` chega a produção sozinho no merge em `main`, e o card 3.6 já criou o precedente do seed-como-migração, que faz da carga-como-migração o caminho mais natural. **Lista permitida, não proibida** (`unidade`, `perfil`, `permissao`, `perfil_permissao`, `usuario`, `usuario_perfil`, `parametro`, `metodo`): tabela nova nasce protegida sem ninguém se lembrar de nada. O varredor **remove o corpo de `create function`** (o `insert into pendencia` do card 5.5 é legítimo: roda quando alguém chamar) mas **não** remove blocos `do $$`, que executam na hora da migração — e **segue as chamadas**, transitivamente, de modo que `create function fn_seed_x(); select fn_seed_x();` reprova pelo que a função escreve, com o caminho `fn_a() → fn_b()` no motivo. `execute` montado em concatenação e `$$` não fechado reprovam por não serem legíveis (fail-closed). ✅ **Exercitado**: 16 asserções sobre 13 SQLs sintéticos e verde nas quatro migrações que já existem, com o **seed do 3.6** — o caso limite, que grava dado permitido — conferido tabela a tabela. Divergência registrada: `usuario_perfil` entrou na lista da nota do card porque `fn_seed_direcao_inicial` escreve nela, e sem isso o seed que o próprio card manda exigir verde ficaria vermelho.

- 02/09/2026 — **card 4.1 concluído, abrindo a Fase 4 de verdade: o catálogo curricular existe.** Migração `20260902180000_catalogo_curricular.sql` — sete tabelas (`metodo`, `material`, `curso`, `curso_material`, `modulo`, `combo`, `combo_curso`), sete triggers de auditoria, RLS habilitada, forçada e **com as políticas no mesmo arquivo** (diferente do card 3.3, onde a janela sem política foi aceita de propósito porque `tem_permissao` ainda não existia; aqui não há o que esperar, e tabela publicada no PostgREST sem política é uma API aberta pelo tempo que a tarefa seguinte durar). Seis das sete nascem **vazias**: o catálogo real é dado de planilha e entra pelo importador do card 9.1, no ambiente dev. A exceção deliberada é `metodo` — enumeração fixa do produto, fechada no `check` da coluna e já referenciada pelos parâmetros `ritmo_padrao_dias_*` do seed do 3.6 —, escrita por `fn_seed_metodos(unidade)`, **a mesma função que o `supabase/seed.sql` chama para as duas unidades da fixture**: é a lição da camada `acesso_seed_real` aplicada de novo, porque enumeração declarada duas vezes é enumeração livre para divergir. Idempotência `do nothing` e não `do update`: a matriz do card 2.4 dá `materiais.editar` sobre `metodo`, então o nome é editável na tela do card 4.4, e `do update` faria a correção feita lá voltar ao valor da migração no deploy seguinte — a mesma falha que o card 3.6 corrigiu no contrato do seed. Junto vem a **camada `catalogo_curricular` da escola-fixture** (o portão do card 3.4.5 não deixaria este card fechar verde sem ela): seis materiais, quatro cursos, três módulos e três combos por unidade, com os códigos de material **repetidos entre métodos** de propósito — é assim na planilha, e é o que faz a fixture exercitar `material_codigo_uk (unidade_id, metodo_id, codigo)` em vez de passar igual se a unique estivesse escrita em `(unidade_id, codigo)`. ✅ **Exercitado, não lido**: `supabase start` do zero aplicando as cinco migrações e o seed limpo, suíte **138/138 `Result: PASS`** (eram 115), portão de migrações verde apontando `metodo` como a única escrita — e cada asserção nova conferida **vermelha** antes de ser aceita, num arquivo de prova temporário que quebrou de propósito a política, o código de permissão, o trigger de auditoria, o `unidade_id` e a permissão do monitor. Duas correções de fato vieram daí, as duas de premissa errada: **`set constraints … immediate` numa constraint DEFERRABLE não volta à checagem linha a linha** — ela passa a valer no fim do *comando* —, então a contraprova do `deferrable` teve de ser reescrita contra uma unique `NOT DEFERRABLE` de verdade; e **o portão do card 4.0,5 lia a PRIMEIRA definição de cada função**, o que deixaria uma migração futura substituir por `create or replace` uma função inofensiva por uma carga do catálogo e ainda assim passar — corrigido para a última definição aplicada, com as duas asserções novas e a primeira vista vermelha contra o código antigo (`docs/ci-cd.md` §14).

- 02/09/2026 — **card 4.3 concluído: a infraestrutura física existe, e a senha do PC deixa de ser um problema em aberto.** `supabase/migrations/20260902233000_infraestrutura_fisica.sql` cria `sala`, `pc`, `pc_manutencao`, `professor` e `pc_credencial_acesso` (auditoria, RLS habilitada e **forçada**, as 16 políticas do card 2.4 §4) e implementa a política do card 2.9: `fn_pc_credencial_gravar`, `fn_pc_credencial_ler` e o trigger que não deixa segredo órfão no Vault. **Estrutura e mais nada, e nenhuma senha**: as salas, os PCs e os professores reais entram pelo importador do card 9.1, no ambiente dev, e as credenciais atuais estão queimadas (card 2.9 §1.5). Suíte nova `031_infraestrutura_fisica.sql` (50 asserções) e camada `infra_fisica` da escola-fixture. Três achados. (1) **A cascata de uma FK não passa pela RLS da tabela referenciadora** — medido: apagar um PC levava junto, em silêncio, a manutenção registrada **e o log de quem leu a senha**, duas tabelas que não têm política de `delete` para ninguém; ou seja, a exclusão apagava exatamente a prova que o card 2.9 §6 elegeu como o único controle que sobrevive a três pessoas terem a permissão. Fechado com `tg_pc_exclusao_valida` (PT409 / **`PC_COM_HISTORICO`**, contrato de **26 → 27**), que faz o "excluir sala/PC **sem histórico**" do catálogo do card 2.4 deixar de ser texto e virar estrutura; PC digitado errado continua apagável, PC que já rodou sai por `status = 'DESATIVADO'`. A contraprova está dentro da suíte: o trigger cai na própria transação e a cascata apaga o histórico. (2) **A pergunta que o card 2.9 §12 deixou em aberto tem resposta, e o desenho fica de pé**: `postgres` **enxerga** `vault.decrypted_secrets`, e `authenticated`/`anon` não têm nem `usage` no schema `vault` — virou o teste **C15**, ao lado do **C14**, que reprova qualquer coluna de `public` cujo nome case com `senha|password|pwd|secret` (a exceção é nominal: `credencial_secret_id`). Sem C14, uma migração futura acrescentaria `credencial_senha text` e passaria por toda a suíte, devolvendo a senha ao `pg_dump` semanal em texto puro. (3) **Não havia o que "habilitar"**: o ajuste #2 do card 2.9 pedia `create extension supabase_vault` na migração, e a extensão **já vem de fábrica** em todo projeto Supabase — a migração passou a **exigir** em vez de criar, o que num Postgres sem a casca da plataforma (o alvo do ensaio de restauração do card 3.11) para o `db push` com a causa escrita em vez de deixar as duas funções nascerem quebradas. Divergências registradas: o §7 do card 2.9 batiza a **função** de limpeza como `tg_pc_credencial_apaga`, e neste projeto `tg_` é prefixo de trigger — ficou `fn_pc_credencial_apagar`; e o `DETAIL` de `PC_INEXISTENTE` passou de texto puro a `json_build_object`, fechando o ajuste que o card 3.7 apontava para cá. ✅ **Exercitado, não lido**: `supabase start` do zero com as **sete** migrações e o seed limpos, suíte **246/246 `Result: PASS`** (eram 194), portão de migrações verde ("nenhuma escrita de dado") e **dez contraprovas** vistas passando contra o código quebrado de propósito — entre elas a que mais importa, **sem o filtro de unidade no corpo da função `definer`, a direção da segunda unidade LÊ a senha de um PC da primeira**, porque `definer` de dono com `BYPASSRLS` ignora a RLS por inteiro. ⚠️ O lado Flutter (uma entrada no catálogo de erros) **não foi exercitado nesta sessão**: não há Flutter na imagem, e o portão é o job `app` do CI, na versão fixada 3.47.2 — que ficou verde. ✅ **Reconferido no projeto dev depois do `db push`** (run `33687935567`): as cinco tabelas com RLS **forçada**, **16 políticas**, **zero linhas** nas cinco, e o Vault exatamente como o desenho exige — `postgres` com `usage` em `vault` e `select` em `vault.decrypted_secrets`, `authenticated` e `anon` sem nenhum dos dois. A pergunta do card 2.9 §12 fica respondida no projeto que serve o app, e não só no stack local.
- 02/09/2026 — **card 4.4 concluído: a primeira tela de negócio do app — Materiais, cursos, módulos e combos.** `app/lib/telas/materiais/` (três abas na rota `/materiais`: materiais com filtros e formulário, cursos com sequência de apostilas reordenável e módulos no Modular, combos com os cursos na ordem), `app/lib/catalogo/` (modelos, filtros e o **plano de gravação de uma sequência** como lógica pura; repositório PostgREST nas tabelas, nunca em view) e dois componentes do design system que nasceram aqui e não no 4.6 — **`TabelaIm360`** (§5.2: os quatro estados num contrato só, degradação por prioridade de coluna, cartões no mobile) e **`FormularioIm360`** (§5.4). Decisões: a sequência ordenada grava-se em até três requisições (apagar → atualizar posições num único `upsert` → inserir), e a troca de posições é exatamente o caso que o `deferrable` das constraints de ordem existe para permitir — **medido contra o stack local**; `23503` e `23505` ganharam tradução própria no app (a constraint é a regra, e sem tradução virariam evento no Sentry a cada tentativa); o método não muda depois de criado; categoria é texto livre com sugestão. ✅ **Exercitado, não lido**: `supabase start` local com as sete migrações e o seed, build de release logado como `secretaria@escola-a.test` — criação, código duplicado (banner do 23505), exclusão em uso (409/23503), exclusão limpa, módulo novo (201) e a troca de ordem num `upsert` único conferida no banco pela API. **Achado que só o banco real mostrou:** depois de salvar, o painel voltava à ordem antiga com o botão aceso, porque durante a recarga o `AsyncValue` ainda carrega o valor anterior — corrigido, e o repositório falso ganhou atraso de leitura para o teste reproduzir (visto vermelho antes de aceito). Suíte Flutter **173 verdes** (eram 117), `analyze --fatal-infos` e `dart format` limpos; sem migração. ⚠️ Ajuste que o DDL precisa receber: nada no banco impede `curso_material`/`modulo`/`combo_curso` de cruzarem métodos — trigger de coerência para o card 6.1.

- 03/09/2026 — **card 4.6 concluído: a terceira tela de negócio — Alunos, lista e ficha.** `app/lib/telas/alunos/` (rota `/alunos`: lista com busca por nome/código SGF, método, status, combo e o chip "Ocultar formados e cancelados"; **ficha como página** em `/alunos/:id` com cabeçalho, "Alterar status", "Editar dados" e as abas Dados e Histórico — Trilha, Turmas e Certificado ficam no lugar dizendo qual card as entrega) e `app/lib/alunos/` (modelos, lógica pura, repositório PostgREST nas tabelas + as duas RPCs de status, providers). Nasceram o **`BadgeStatus`** (§5.1, o componente que o §9 do card 2.7 reservava para cá), a célula em widget da `TabelaIm360`, o `aoErro` do `FormularioIm360` e `lib/util/datas.dart` (as funções de data saíram de `infraestrutura/`, que as reexporta). Decisões: (a) **o menu "Alterar status" oferece só as transições válidas a partir do status atual** — a tabela de `fn_aluno_transicao_valida` copiada como *forma*, com teste que a compara à do banco; quem decide continua sendo o trigger, e FORMADO → CANCELADO entra porque o banco aceita; (b) **a tela não pré-valida o motivo**: submete, e o `MOTIVO_OBRIGATORIO` do banco vira banner **e** realce do campo (design-system §5.4) — na reversão o motivo é sempre obrigatório e aí é formato, validado localmente; (c) **reverter mora no Histórico**, e "Alterar status" num status sem transição comum fica visível e desabilitado com o motivo; (d) **o método não muda depois da matrícula** e o combo é filtrado pelo método, com o aviso do §7.3 ao trocá-lo; (e) **a lista lê a tabela `aluno`** e resolve método e combo pelo catálogo já carregado — `v_aluno_lista` fica para quando houver turma a juntar (Fase 5), e a coluna Turmas/⚠ do wireframe é do 5.7. ✅ **Exercitado contra o stack local (7 migrações + seed), não lido**, como direção: lista com os dez em curso; STANDBY sem motivo devolveu `PT422/MOTIVO_OBRIGATORIO` e virou banner + campo vermelho; com motivo, `fn_aluno_alterar_status` gravou (status, `status_desde`, linha de histórico com motivo — tudo conferido pela API); código SGF duplicado devolveu 23505 no banner; edição de dados (PATCH) sem tocar o status; matrícula (POST) nascendo ATIVO com data de hoje; reversão de CANCELADO pela aba Histórico. Suíte Flutter **236 verdes** (eram 207), `analyze --fatal-infos` e `dart format` limpos; **sem migração**. ⚠️ Achado medido: **o histórico mostra "quem" só quando a política de `usuario` deixa** — a secretaria lê `usuario: null` na transição feita pela direção (só `admin.ler` e o próprio usuário leem `usuario`, card 3.4); a ficha omite o nome em vez de mentir. Fecha o apontamento do card 4.5 (largura máxima do diálogo de confirmação).
- 02/09/2026 — **card 4.5 concluído: a segunda tela de negócio — Salas e PCs, com Professores.** `app/lib/telas/salas/` (rota `/salas`: aba de salas com nominal, PCs operacionais/total e capacidade efetiva; painel da sala com os PCs, a situação de cada um e a ação contextual da linha — **Manutenção / Encerrar / Reativar**, cada uma num formulário pequeno; ficha do PC com a **credencial do card 2.9 §8**; segunda aba de professores, que não se excluem) e `app/lib/infraestrutura/` (modelos, lógica pura, repositório PostgREST + as duas RPCs de credencial, providers). Dois widgets saíram da tela 4.4 para `lib/widgets/` porque a segunda tela precisou deles (`confirmarEfemero`, `PainelDetalhe`/`TituloSecao`), e `mensagemNadaExcluido` foi para `erro_app.dart`. Decisões: (a) **a capacidade efetiva da sala é derivada na tela e informativa** — PCs OPERACIONAIS até o teto nominal, a mesma conta que `fn_capacidade_efetiva` (5.2) fará por bloco; "Blocos desta sala" fica para a grade do 5.6, e a tela diz isso; (b) **`pc.status` e a manutenção em aberto são duas colunas que só o card 5.4 amarra**: até lá, quem tem `salas.editar` escolhe no formulário se o status acompanha (ligado por padrão), e o monitor registra a manutenção com um aviso de que o status não muda — a regra continua fora do Dart; (c) **`data_fim` é previsão**: manutenção aberta = sem fim ou fim à frente; "Encerrar" grava o fim de hoje; (d) **a credencial vive só no diálogo**: a leitura acontece ao abrir (uma linha em `pc_credencial_acesso` por abertura), `CredencialPc.toString` não imprime a senha, gravar e rotacionar são o mesmo formulário com os dois campos em branco; (e) datas `dd/mm/aaaa` em campo de texto, sem `intl` e sem localização — validação local só de formato; (f) status do PC em texto, como a situação no 4.4 — o `BadgeStatus` é do 4.6. ✅ **Exercitado contra o stack local (7 migrações + seed), não lido**: como secretaria, registrar manutenção com previsão de fim (status foi a MANUTENCAO, efetiva caiu para 3), encerrar (data_fim = hoje, status voltou), reativar o desativado, PC novo de dentro do painel nascendo na sala certa, professor novo e **nome duplicado devolvendo 23505 no banner**; como direção, exclusão limpa, credencial gravada, lida no diálogo e **uma** linha de log com o `criado_por` da direção — tudo conferido pela API depois de cada passo. O `PC_COM_HISTORICO` foi provado pelo REST (PT409 com o código no `details`) e pelo widget test com esse payload. Suíte Flutter **207 verdes** (eram 173), `analyze --fatal-infos` e `dart format` limpos; **sem migração**. ⚠️ Ajuste para o card 5.4: trigger amarrando `pc.status` a `pc_manutencao` (abrir → MANUTENCAO; encerrar → OPERACIONAL), para o interruptor do formulário deixar de existir. Corrigida de passagem a tabela de `docs/permissoes-matriz.md`, que não listava `salas.acessar_credencial`.

- 03/09/2026 — **cards 4.7 e 4.7.5 concluídos: a tela de Administração, a única Edge Function da v1 e o histórico da matriz** (branch `tarefa/4-7-tela-administracao`, **COM migração** `20260903100000_matriz_historico.sql`). `app/lib/telas/administracao/` (Usuários, Perfis e matriz, Parâmetros, Histórico) e `app/lib/administracao/`; `supabase/functions/convidar-usuario/` com o contrato do card 3.5 §3.2 e a lógica pura testada por `node --test`; `docs/administracao.md` é a fonte. Três achados do card 3.8 fechados: **o convite passa a terminar na tela de definir senha** (o `type=invite` do fragmento é lido em `main` **antes** do `Supabase.initialize`, que o consome e limpa — depois disso convidado e login são indistinguíveis), **quem está sem perfil aparece** na lista e num aviso, e **os códigos do GoTrue** (`over_email_send_rate_limit`, `email_exists`…) ganharam texto. O 4.7.5 ficou com a **tabela por trigger** e não com `ativo = false` (a segunda mudaria três lugares já asseridos para guardar uma transição só): `perfil_permissao_hist` imutável, FKs `restrict` (perfil com histórico não se apaga), e `fn_seed_matriz` deixa de devolver o código que alguém tirou de todos — o caso residual do card 3.6. ✅ **Exercitado contra um stack local próprio** (sessão paralela ao card 4.6 no mesmo repositório: worktree + cópia de `supabase/` com portas 553xx): convite pela Edge Function (200 com `usuario_id`; 403 `SEM_PERMISSAO` como monitor; 401 sem token; 400 e-mail inválido; **reenviar a quem não aceitou devolve o mesmo usuário, não `email_exists`**), e-mail no Mailpit, link seguido até *"Defina sua senha para concluir o cadastro"*, senha definida, *Entrar no sistema* caindo no menu de MONITOR; matriz marcada/desmarcada pela tela com o histórico registrando quem; parâmetro editado com o aviso da rotina e `atualizado_por` conferido pela API; insert direto no histórico recusado (42501). Suíte do banco **266/266** (eram 246), Flutter **250** (eram 207), funções 6/6, portão de migrações verde. ⚠️ Registrado em `docs/administracao.md` §6: convite pelo painel continua entrando sem perfil; `usuario_perfil` sem histórico próprio.

- 03/09/2026 — **card 4.8 (marco 1) com as pré-condições cumpridas; o marco em si aguarda a sessão com a secretaria.** As quatro pré-condições verdes: suíte do banco **266/266**, `flutter analyze` limpo, `flutter test` **279/279**, `deploy-web` verde em `3d17ae5a` com `homolog.gestaoim360.com` no ar, e as 8 migrações aplicadas no dev **pelo pipeline**. Pré-verificados contra o banco os critérios 1, 2, 3, 5 e 6 (passam) e o 4 sem falha local: as duas camadas do card 2.8 §6.1 devolvem `PT409`/`TRANSICAO_INVALIDA` — a RPC **e** o `PATCH` cru no PostgREST —, o monitor recebe `PT403`/`SEM_PERMISSAO`, e as três transições do roteiro gravaram `aluno_status_hist` com quem e quando. **Três achados.** (1) **O critério 1 estava errado, não o sistema**: pedia 49 códigos e monitor 13, e o certo é **50 / direção 50 / secretaria 37 / pedagógico 22 / monitor 14** desde o card 2.9 (`salas.acessar_credencial`) — valor que `permissoes-matriz.md` §5 já registrava e que a cópia em `estrategia-testes.md` §15.1 não acompanhou; medido no dev e corrigido, porque **critério de aceite errado reprova software correto**. (2) Duas pré-condições que o enunciado não previa e são de Irineu: **dev tem um usuário só** (os quatro perfis vivem na escola-fixture, que nunca sai da máquina local) e **os cadastros do roteiro vêm antes dos quatro logins** (dev está sem dado de negócio por decisão, então lista vazia lá é falta de dado, não RLS) — as duas escritas no §15.1 para M2, M3 e M4 encontrarem. (3) A suíte local reprovou 5 arquivos por **volume restaurado**, não por regressão: `supabase start` restaura o volume anterior, que era de antes da migração do 4.7.5; `stop --no-backup` + `start` fechou 266/266.
- 03/09/2026 — **card 4.7,6 concluído: os dois e-mails do Auth em português e formatados.** Chegavam no template padrão do Supabase ("You've been invited" / "Accept invitation"), em inglês e sem formatação; o remetente já era o certo desde o card 3.8. Entregues `supabase/templates/convite.html` e `recuperacao-senha.html` (ligados em `[auth.email.template]`, que vale **só** no stack local), o aplicador `supabase/templates/aplicar-templates.mjs` para os painéis remotos, `docs/emails-auth.md` como fonte e o wordmark rasterizado em `app/web/marca/gestao-im360-horizontal.png` — e-mail não renderiza SVG. **A restrição que mandou no card:** o link é `{{ .ConfirmationURL }}` e não pode virar `{{ .Token }}`, porque o convite volta com a sessão no **fragmento** com `type=invite`, que é o que `main.dart` lê antes do `Supabase.initialize` (card 4.7) — link por token reabriria o defeito do 3.8, a pessoa entrando sem senha cadastrada. Decisões: o layout tem de ficar **completo com a imagem bloqueada**, que é o caso esperado num primeiro contato (identidade na barra e no título; estilo tipográfico no `img`, porque o cliente aplica o CSS do `img` ao `alt`); **um** endereço de imagem para os dois ambientes, o de produção, para os templates serem byte a byte iguais e a conferência ser possível; marca e ação em tons distintos como o guia manda; e o convite dizendo que falta **definir a senha**, com o botão "Definir minha senha". ✅ **Exercitado no Mailpit, não lido**: convite com nome, convite sem nome (o ramo `else`, que é como o painel convida), recuperação, `type=invite` e `type=recovery` no HTML entregue, nenhuma chave Go não resolvida, e o layout renderizado também sem a imagem. 279 testes Flutter verdes, `analyze` limpo, **sem migração**. ⚠️ Dois achados: **o GoTrue lê o template no boot, não no envio** (editar e mandar entrega a versão antiga, sem erro — `stop` + `start` antes de testar), e `{{ .ConfirmationURL }}` sai com escape HTML, que é o correto nos dois lugares. ⚠️ **Falta o que é de Irineu:** aplicar nos dois painéis; até lá dev e prod seguem no template em inglês.

- 03/09/2026 — **card 4.9 concluído: a data de entrega recalibrada com telas de negócio medidas, e o gargalo mudou de lugar.** Segunda rodada do método de `docs/estimativa-entrega.md`; números na subpágina Notion **Estimativa de entrega** do card 3.13, que segue sendo o **único** lugar onde a data mora. **P50 = 07/10/2026, P80 = 28/10/2026** para o go-live (era 20/10 e 30/11): a banda encolheu de 41 para 21 dias e, pela primeira vez, **P50 e P80 cabem os dois dentro do alvo de outubro/2026** — para o go-live, outubro deixou de ser alcançável e passou a ser seguro (as lojas não: P80 = 02/12/2026). Medidos 198 pontos em 6 dias, com `Tela` saltando de 8 para **37 pontos**. Três achados: (1) **o risco que o P80 precificava não se materializou** — as quatro telas de negócio saíram no tamanho atribuído e em 03/09 duas telas `GG` foram entregues no mesmo dia, então o P80 passa de P50 ÷ 2 a P50 ÷ 1,5, exatamente como o §4 do método previa; (2) **a latência passou o esforço** — no P50 são ~16 dias corridos de esforço contra ~19 de latência, e subir a capacidade de 3,0 para 7,0 dias-plenos por semana (o ritmo de fato observado) antecipa o P50 em só 9 dias: **daqui em diante a data se ganha encurtando marco, revisão do pedagógico e treinamento, não codando mais rápido**; (3) **a premissa "escopo congelado" era falsa e virou desconto medido** — 25 pontos de cards novos em 6 dias (~4,2/dia), agora subtraídos da velocidade bruta, e é isso que faz o P50 ser 26 e não 33. Passo 2 executado com oito redimensionamentos, saldo **+9 pontos** (5.7 `G`→`GG`, 6.6 `G`→`M`, 7.4 `M`→`P` e os **seis cards de recalibração** `P`→`M`): telas e schemas se sustentaram e nenhum `GG` precisou ser quebrado — o que não se sustentou foi o tamanho dos próprios cards de recalibração, medido nesta sessão. Fica anotado o viés: o único grupo redimensionado para cima **por medição direta** foi o que a própria rodada executou; os demais foram julgados por analogia, que é mais gentil. Fica anotado o que hoje é o número menos confiável do modelo: **a tabela de blocos de latência nunca foi medida contra a realidade**, e o marco 4.8 é o primeiro teste dela.

- 03/09/2026 — **card 4.7,7 concluído: a tela passou a dizer que convidar de novo reenvia o convite** (branch `tarefa/4-7-7-reenviar-convite`, **COM migração** `20260903170000_convites_pendentes.sql`). O achado é de Irineu, ao preparar o marco 4.8: procurou como reenviar um convite e concluiu que não dava — mas o reenvio funcionava desde o card 4.7. **O defeito era de descoberta, não de comportamento**, e é uma família própria: função entregue, testada e invisível vale zero, e o sintoma é a pessoa desistir em silêncio. A lista agora marca `Convite pendente` na coluna Situação e a ficha da pessoa traz **"Reenviar convite"** — na linha, que é onde se procura por isso, e não num botão chamado "Convidar usuário"; o formulário de convite também passou a dizer, em uma frase, que o mesmo e-mail reenvia. O estado vem de `fn_convites_pendentes()`, `security definer` porque `auth.users.email_confirmed_at` nenhum papel do app alcança. **A decisão que sustenta o card:** o pivô é `email_confirmed_at` porque é literalmente o que o GoTrue usa para escolher entre reenviar e recusar com `email_exists` — a lista não é uma aproximação de "ainda não aceitou", é a resposta a *"para quem o botão funciona"*. Função e não coluna espelhada, porque coluna divergiria do Auth **em silêncio**; e a marca exige `admin.ler` (a permissão da LISTA) enquanto o botão exige `admin.gerir_usuarios`, senão quem só lê veria a lista sem marca nenhuma — indistinguível de "todo mundo já aceitou". ✅ **Exercitado, não lido**, e os dois lados do par: convite real deixa `email_confirmed_at` nulo e o id aparece; reenviar devolve o **mesmo** id e ele continua na lista; confirmar o e-mail tira o id da lista **e** faz o convite seguinte devolver `422 email_exists`; monitor (sem `admin.ler`) recebe `[]`; anônimo recebe `42501`. Depois, pela tela contra a Edge Function de verdade: "Convite reenviado." e o segundo e-mail no Mailpit. **Dois defeitos que só o exercício encontrou:** (1) o rodapé do `FormularioIm360` era uma `Row` com `Spacer` e **estourava** com um botão de apoio a mais — cada botão recebia a largura natural dele; virou `Expanded` + `Wrap`, que quebra a linha em vez de sumir na borda; (2) a coluna Situação tinha `flex: 1` e entregava `Convite p…`, isto é, **a marca do card chegando pela metade** — `larguraMin` só decide se a coluna SAI, quem reparte a largura é o `flex`. Suíte do banco **270/270** (eram 266), Flutter **285/285** (eram 279), `analyze` limpo, portão de migrações verde.

- 03/09/2026 — **card 5.1 concluído, abrindo a Fase 05: os blocos de horário e a alocação existem.** `supabase/migrations/20260903190000_blocos_alocacao.sql` cria `bloco_horario`, `bloco_aluno` e `bloco_aluno_reposicao` (auditoria, RLS habilitada e **forçada**, as dez políticas do card 2.4 §4 — as duas de alocação sem `delete`, porque alocação encerrada é `ativo = false` e reposição desmarcada é `status = 'CANCELADA'`), com os dois bloqueantes do card 2.5 nascendo prontos: **`tipo_desde`** (o relógio que a virada REP zera) e o valor **`FALTOU`** no `check` do status. **Estrutura e mais nada:** blocos e alocações reais entram pelo importador do card 9.1, no ambiente dev; o portão do 4.0,5 confirma. Suíte nova `040_blocos_alocacao.sql` (43 asserções) e camada `turmas` da escola-fixture — três blocos com 0, 9 e 10 alunos, **disjuntos**, porque reaproveitar os mesmos alunos nos dois faria nove ATIVO ficarem com dois blocos, que é a definição de aceleração. **Três decisões e um portão que reprovou.** (1) **`tipo_desde` é derivada e não editável**: no `UPDATE` o valor enviado é ignorado e só uma mudança de `tipo` a move — editável pelo PATCH, ela seria o contorno da virada REP (para trás o débito volta a pesar, para a frente some); no `INSERT` o valor informado vale, para o importador do 9.1 não zerar o relógio de toda alocação migrada. (2) **A folga que o card 4.2 nomeou existe mesmo aqui**: o `or` da política de update de `bloco_aluno` está lá só para a desalocação sem ator escrever `ativo = false`, e como RLS não é por coluna ele autorizava junto `tipo` (executar a virada), `bloco_id` (trocar de turma sem checar vaga) e reativar quem foi trancado; fechado com guarda no trigger nas duas tabelas — em `bloco_aluno_reposicao` só `CANCELADA` passa sem `turmas.alocar`, senão quem altera status quitaria um débito marcando `REALIZADA`. (3) **A cascata volta a ser o caminho aberto**, como o card 4.3 previu ao nomear esta tabela: `bloco_aluno.bloco_id` é `on delete cascade` e a tabela não tem `delete` para ninguém, então apagar um bloco levava junto, em silêncio, quem esteve na turma — enquanto `bloco_aluno_reposicao` é RESTRICT e morria num `23503` ilegível: **a mesma operação com dois desfechos opostos**. Fechado com `tg_bloco_exclusao_valida` (PT409 / **`BLOCO_COM_ALOCACAO`**, contrato de **27 → 28**), e bloco digitado errado continua apagável. **O portão:** a suíte 030, escrita pelo card 4.2, declara `tg_aluno_status_desaloca` devido **neste** card, no dia em que `bloco_aluno` nascer — e reprovou na primeira execução da migração. A nota do board o punha no 5.3; resolvido a favor do portão, pelo motivo que ele traz escrito: **sem ele o aluno em STANDBY continua ocupando vaga toda semana, sem erro nenhum**, e a partir desta migração já existe vaga para ocupar. A terceira tabela que ele deve tocar (`turma_modular_aluno`, card 7.1) ganhou portão próprio no teste 040, com a prova por construção de que reprova. ✅ **Exercitado, não lido**: `supabase start` do zero com as **dez** migrações e o seed limpos, suíte **313/313 `Result: PASS`** (eram 270), `flutter test` 285/285, `analyze` e `format` limpos, portão de migrações 18/18 e verde nas dez; quatro contraprovas vistas no mundo sem cada guarda (a virada REP executada pelo PostgREST, o relógio do débito editável, o débito quitado com `REALIZADA`, o aluno em STANDBY ocupando vaga); e o REST de verdade devolvendo `409` com `BLOCO_COM_ALOCACAO` no `details`, `42501` no monitor que aloca, e `tipo_desde` ignorando o PATCH e carimbando hoje quando o `tipo` muda.

- 03/09/2026 — **card 5.2 concluído: a capacidade do bloco existe, e sai dos PCs.** `supabase/migrations/20260903210000_capacidade_vagas.sql` traz `fn_capacidade_efetiva`, `fn_ocupacao_bloco` e `fn_vagas_livres` — **as três**, e não só a primeira como dizia o mapa §13 do card 2.2: capacidade sem ocupação não vira vaga, e o §10 (#3) do card 2.3 e a fixture do 5.1 já atribuíam as duas primeiras a este card (divergência registrada, e as Notas do 5.3 corrigidas). Só função: nenhuma linha gravada, portão do 4.0,5 verde. **A fórmula, que era o item 2 do "o que fica em aberto" do card 2.2, e o ponto de partida dele tinha dois furos** — os dois só visíveis lendo o §4.1 junto com o §4.6, cada metade sozinha parecendo certa. (1) **O `status = 'OPERACIONAL'` matava o substituto**: o `tg_pc_manutencao_status` do card 5.4 põe o PC em `MANUTENCAO` com substituto ou sem, e a partir daquele dia "um `pc_substituto_id` mantém a capacidade" deixaria de valer **em silêncio, num card que não fala de capacidade**. (2) **Substituto da própria sala não cria máquina**: apontar para um PC que já está na sala somaria a mesma duas vezes — dez PCs físicos valendo onze vagas, com a cara de um número certo. Final: conta o PC que (a) não está `DESATIVADO`, (b) não tem manutenção cobrindo a data sem substituto **de outra sala** e (c) está `OPERACIONAL` ou tem manutenção cobrindo a data **com** esse substituto; depois `least(…, capacidade_nominal)`, e `capacidade_override` vence sempre. Correta **antes e depois do 5.4**, que é o requisito de verdade. Mais duas decisões: **`security definer` com filtro de unidade no corpo** (a razão do card, do §10 #3 — como invoker, quem não tem `salas.ler` contaria zero PCs e veria a grade inteira **lotada**, sem erro nenhum, porque a RLS nega linha), e **nulo ≠ zero** (`0` é bloco vazio; nulo é bloco de outra unidade) — ⚠️ com a armadilha do `greatest(null, 0)`, que devolve **0** porque ignora nulos e faria `fn_vagas_livres` dizer "lotado" para um bloco que ela nem deveria enxergar. Suíte nova `041_capacidade_vagas.sql` (33 asserções). ✅ **Exercitado, não lido**: stack local do zero com as **onze** migrações e o seed, suíte **346/346 `Result: PASS`** (eram 313), portão de migrações 18/18 e varredor verde. **Dois achados de bancada.** (a) **O C6 não enxergava default de parâmetro**: ele lê `prosrc`, e o default mora em `proargdefaults` — `fn_capacidade_efetiva` é a primeira função do projeto com parâmetro de data, e o §4.1 a escrevia com `current_date`; o teste ganhou a quarta origem e agora reprova. (b) **`limit 1` sem `order by` no teste 040**, escrito pelo card 5.1: ele escrevia uma alocação inativa para um aluno **sorteado**, e no primeiro stack local em que o sorteio caiu no `Aluno de Lotação 13` o arquivo inteiro morreu em `more than one row returned by a subquery`, 27 dos 43 testes sem rodar, com a mensagem 60 linhas adiante da causa — verde no CI o tempo todo. Corrigido por chave natural e virou linha na tabela de determinismo (§11).

- 03/09/2026 — **card 5.3 concluído: a vaga passou a ser disputada de verdade, e a virada REP tem quem a execute.** `supabase/migrations/20260903230000_admissao_lotacao_rep.sql` traz as duas camadas da admissão — os triggers-garantia (`tg_bloco_aluno_admissao`, `tg_reposicao_admissao`) e as funções de aplicação (`fn_bloco_admitir`/`fn_bloco_remover`, `fn_reposicao_agendar`/`registrar`/`cancelar`) — mais `tp_rep_situacao` e as quatro funções do card 2.5 (`fn_rep_situacao`, `fn_rep_avaliar_virada`, `fn_rep_virar_continuo`, `fn_rep_voltar_pontual`). **Só função, trigger e uma coluna**; portão do 4.0,5 verde. Quatro códigos novos (contrato de **28 → 32**): `BLOCO_INEXISTENTE`, `ALOCACAO_INEXISTENTE`, `REPOSICAO_INEXISTENTE`, `REPOSICAO_NAO_PREVISTA`. **Cinco decisões, e as três primeiras nascem do mesmo lugar — o nulo.** (1) **Capacidade nula é ERRO, não "sem opinião"**: as funções do card 5.2 devolvem nulo para bloco de outra unidade, e `ocupacao >= null` é *nulo*, então o `if` escrito sem cuidado não dispara e a lotação passa **em silêncio** exatamente na escrita que não deveria existir — medido, com um `insert` como `postgres` (que tem BYPASSRLS) entrando sem checagem de vaga nenhuma. (2) **A vaga só é disputada quando a linha ENTRA na conta** (insert ativo, volta de inativa, troca de bloco): conferir em todo `update` faria mudar só o `tipo` de uma alocação já ativa responder `BLOCO_LOTADO` num bloco que não mudou de tamanho — e é isso que a virada REP dentro do próprio bloco do aluno faz. (3) **O trigger BEFORE fala antes do `check`, da `unique` e do WITH CHECK da RLS**, então toda asserção de camada 1 escrita com dado inválido para a camada 2 passa a medir a camada 2 — três asserções do teste 040 tiveram de ser reescritas por isso, e a lição vale para todo card de trigger daqui em diante. (4) **`fn_bloco_remover` ganhou onde gravar o motivo** (`bloco_aluno.motivo_saida`): o `p_motivo` do card 2.2 §4.3 e o `MOTIVO_OBRIGATORIO` de `fn_rep_voltar_pontual` exigiam do usuário um dado que se descartava, e `tg_aluno_status_desaloca` passou a escrever ali o status que tirou o aluno da turma. (5) **A escola-fixture passou a escrever a camada `turmas` em CONTEXTO DE ROTINA** em vez de o trigger ganhar uma exceção para "quando não há sessão": a alternativa seria um contorno permanente em produção escrito para acomodar um arquivo de teste, e o efeito colateral é o melhor da mudança — **a fixture passou a ser validada pelas regras de verdade**, com um décimo primeiro aluno reprovando o `db reset` em voz alta em vez de criar 11 alunos em 10 PCs para os testes do 5.2 e do 5.3 medirem. ✅ **Exercitado, não lido**: stack local do zero com as **doze** migrações e o seed, suíte **414/414 `Result: PASS`** (eram 346), `flutter test` **285/285**, `analyze --fatal-infos` e `format` limpos, portão de migrações 18/18 e varredor verde. **Cinco sabotagens vistas vermelhas**, cada uma pela asserção certa: `ceil`→`floor` (as duas bordas do card 2.5), débito contado por linha em vez de por aula de origem, guarda de nulo removida, `not old.ativo` removido da condição de entrada, e `fn_exige_permissao('turmas.ler')` removido de `fn_rep_situacao`. E o **primeiro teste de concorrência do projeto está no ar**: `supabase/tests_concorrencia/admissao_ultima_vaga.sh` — duas sessões `psql` simultâneas no último lugar, uma passa e a outra recebe `BLOCO_LOTADO` —, ✅ **com a contraprova**: sem o `pg_advisory_xact_lock` as duas passam e o bloco fecha com **11 alunos em 10 PCs**, e o script reprova por CONTAGEM, não por *timeout*. ⚠️ Divergência registrada: o §6.5 do card 2.8 descreve as bordas em termos de **pendência** aberta com `chave_dedup` `:CONTINUO`, e `pendencia` é do card 5.5 — o teste `085_rep_virada` mede o **veredito**, que é a camada de baixo, e a seção 6 dele é o portão que reprova no dia em que a tabela nascer. Mesmo desenho para o passo 4 do §5.2 do card 2.5 (fechar a pendência dentro de `fn_rep_virar_continuo`).

- 03/09/2026 — **card 5.5 concluído: o sistema passou a dizer o que está errado, e a fazer isso sozinho todo dia.** `supabase/migrations/20260903234500_pendencias_rotinas.sql` traz a tabela **`pendencia`**, as três funções do §10 do card 2.2 (`fn_pendencia_abrir`, `fn_pendencia_resolver`, `fn_pendencia_resolver_id`), a **primeira rotina agendada do projeto** (`rt_diaria` orquestrando `rt_pendencias_diaria` e `rt_rep_avaliar`), o job `pg_cron` e a **primeira view** (`v_pendencias_abertas`). Três códigos novos (contrato de **32 → 35**): `PENDENCIA_INEXISTENTE`, `PENDENCIA_JA_RESOLVIDA`, `RESOLUCAO_INVALIDA`. **Estrutura e mais nada** — e aqui a regra tem uma consequência que vale escrever: **pendência não se migra e não se importa**, é gerada pela rotina a partir do dado de cada ambiente, então em produção antes da virada a rotina roda e não acha nada. **Quatro decisões.** (1) **`fn_pendencia_abrir` e `fn_pendencia_resolver` são `security definer`** (lista fechada do C8): as duas são chamadas como efeito colateral na transação de outro ator, e como `invoker` a RLS as faria devolver "nenhuma linha" e "0 fechadas" **sem erro nenhum** — o sintoma seria a central sugerindo todo dia uma virada REP que já aconteceu, indistinguível de "a rotina não rodou". Com a matriz inicial nada disso apareceria (`turmas.alocar` e `pendencias.resolver` estão nos mesmos três perfis), e é o card 4.2 que deixou escrito que isso não é argumento. O fechamento **humano** (`fn_pendencia_resolver_id`) ficou `invoker` e exige `pendencias.resolver` — é ali que o controle importa. (2) **`on conflict do update`, não `do nothing`**: a descrição carrega os números do dia ("3 aulas a repor; cabem 2 até lá", "10 alunos para capacidade de 9"), e com `do nothing` a pendência aberta na segunda mostraria na sexta os números de segunda — número errado com cara de certo. (3) **A pendência IGNORADA volta na execução seguinte** enquanto a condição valer, porque `pendencia_aberta_uk` é parcial e a dedup só vale com a pendência aberta. É decisão: silêncio permanente por chave seria a falha calada que este projeto cataloga, e o remédio certo para "este bloco pode ficar acima da capacidade" é `capacidade_override`, não esconder o aviso. (4) **`rt_diaria` isola cada rotina num bloco de exceção** e grava a falha como pendência `ROTINA_FALHOU` (ALTA) — e não como log, porque o log do Supabase no free tier tem retenção de **um dia**: uma rotina que falha às 03:10 e some do log às 03:10 do dia seguinte falha em silêncio para sempre, com o sintoma sendo uma central **vazia**. **Dois portões do card 5.3 dispararam e viraram código**: `fn_rep_virar_continuo` e `fn_rep_voltar_pontual` passaram a fechar a sua pendência na mesma transação (passo 4 do §5.2 do card 2.5), e a seção 6 do teste 085 virou a asserção estrutural de que cada uma fecha o **seu** sufixo — trocados, fechariam a sugestão errada em silêncio. **Um portão de terceiro foi corrigido**: o teste 030 declarava `tg_aluno_combo_alterado` devido quando `pendencia` nascesse, e ele **não pode existir** sem `aluno_material` (a trilha, card 6.1/6.2) — condição trocada para a mais tardia das duas, intenção preservada. Nasceram também os catálogos **C5** (toda view `security_invoker`, zero matview) e **C10** (todo tipo passado a `fn_pendencia_abrir` é literal e está no `check`). ✅ **Exercitado, não lido**: stack local do zero com as **treze** migrações e o seed, suíte **463/463 `Result: PASS`** (eram 414), `flutter test` **285/285**, `analyze --fatal-infos` e `format` limpos, portão de migrações 18/18 e varredor verde nas treze. **Seis sabotagens vistas vermelhas**, cada uma pela asserção certa: filtro `tipo <> 'REP'` removido da aceleração, `>` virando `>=` na capacidade, a rotina abrindo e não fechando, a virada não fechando a pendência, severidade `INFO` (que morre no `check` já na primeira execução — o caso que o card 2.3 nomeia) e a chave REP sem sufixo. E **`pg_cron` foi medido, não suposto**: está em `shared_preload_libraries` da imagem do Supabase e o papel `postgres` cria a extensão sem privilégio adicional. ⚠️ **`10 6 * * *` é UTC, isto é, 03:10 em São Paulo** — lido como horário local seria o começo do expediente.
- 03/09/2026 — **card 5.4 concluído: registrar a manutenção passou a mexer na capacidade na hora, e o status do PC deixou de ser escolha de ninguém.** `supabase/migrations/20260904010000_manutencao_pc_capacidade.sql` traz `fn_pc_status_sincronizar`, os dois triggers do §4.6 (`tg_pc_manutencao_status`, `tg_pc_revalida_blocos`), `fn_revalidar_blocos_sala` e as duas rotinas que faltavam a `rt_diaria` (`rt_pcs_normaliza`, `rt_capacidades`) — o portão que o card 5.5 armou no teste 090 cobrava exatamente isso e **reprovava** até elas entrarem. Só função e trigger; portão do 4.0,5 verde. **Cinco decisões.** (1) **`pc.status` é DERIVADO de `pc_manutencao`**, e a regra mora numa função só, usada pelo trigger e pela rotina: duas cópias divergiriam para o lado silencioso, com o trigger acertando e a rotina desfazendo de madrugada. A conta olha **todas** as manutenções do PC e não a linha que disparou o trigger — encerrar uma de duas em aberto devolveria à operação uma máquina ainda parada. (2) **`rt_pcs_normaliza` existe porque o tempo passar NÃO É EVENTO**, e faz as duas direções: manutenção que terminou devolve o PC, manutenção agendada que começou hoje o para — metade da regra deixaria o PC OPERACIONAL enquanto quebrado, que é o erro na direção que vende vaga inexistente. Divergência registrada com o §11: não há manutenção "vencida" a fechar, e escrever em `pc_manutencao` inventaria histórico. (3) **"Sem substituto" na pendência é o mesmo "sem substituto" da fórmula da capacidade** (card 5.2, decisão b): substituto da própria sala não repõe máquina, logo não fecha a pendência — a condição contrária fecharia dizendo "resolvido" enquanto a capacidade seguisse caída. (4) **`BLOCO_ACIMA_CAPACIDADE` voltou ao dono do catálogo §10.1**, `fn_revalidar_blocos_sala`, e saiu de `rt_pendencias_diaria`: manter as duas seria manter duas implementações da mesma comparação, livres para divergir na primeira vez que alguém mexesse numa só. Continuam **dois caminhos** — evento (trigger) e diário (`rt_capacidades`, que `rt_diaria` roda antes) —, convergindo na mesma `chave_dedup`. (5) **A admissão bloqueada não precisou de código novo, e isso foi conferido antes de escrever**: `tg_bloco_aluno_admissao` já compara `ocupacao >= capacidade` desde o 5.3; uma segunda guarda a partir da pendência manteria bloqueado um bloco já normalizado. Virou asserção. ⚠️ **Correção de fato que atravessa app e banco: `data_fim` era lido de duas maneiras.** O app (card 4.5 c) trata o fim como *o dia em que o PC volta*; o banco usava intervalo **fechado**, e o §4.6 dizia "data_fim preenchida e passada". As duas nunca se encontravam — até o status virar derivado: sob a leitura do banco, encerrar a manutenção hoje deixaria o PC parado até amanhã, com a turma da noite perdendo uma vaga por uma máquina que já voltou. Resolvido a favor do app; `fn_capacidade_efetiva` passou a `[data_inicio, data_fim)` e as duas bordas viraram asserção no teste 041. **Fecha a pendência 9.12**: os dois interruptores de status saíram dos formulários do card 4.5, e a tela passou a informar o que o banco faz em vez de oferecer uma decisão que não é mais dela; o repositório falso dos testes de widget reproduz o trigger, senão eles mediriam um mundo que não existe. ✅ **Exercitado, não lido**: stack local do zero com as **catorze** migrações e o seed, suíte **495/495 `Result: PASS`** (eram 463, com o `091_manutencao_capacidade` novo, de 28), `flutter test` **285/285**, `analyze --fatal-infos` e `format` limpos, portão de migrações 18/18 e varredor verde nas catorze. **Oito sabotagens vistas vermelhas** — os dois triggers como `invoker`, o status decidido pela linha do trigger, a pendência aceitando substituto da própria sala, `rt_capacidades` sem a varredura, `rt_pcs_normaliza` pela metade, o intervalo de volta ao fechado e a cópia de `BLOCO_ACIMA_CAPACIDADE` de volta em `rt_pendencias_diaria`. ⚠️ **E DUAS SABOTAGENS SAÍRAM VERDES, as duas corrigindo o que eu tinha escrito**: `fn_pc_status_sincronizar` nasceu `security definer` com a justificativa da RLS, e quem atravessa a RLS é o trigger `definer` que a chama (virou `invoker`, pelo precedente de `fn_pendencias_fechar_ausentes`); e a **ordem** entre os dois triggers de `pc_manutencao`, que eu afirmara ser decisiva, **não importa** — o `update` de `pc.status` dispara a revalidação de novo em `pc`, e a independência vem dessa redundância. A fixture, de quebra, passou a nascer com uma pendência `PC_SEM_SUBSTITUTO` por unidade: **gerada pelo trigger, não semeada**, e é o LAB2-05 que a camada `infra_fisica` criou parado de propósito desde o card 4.3. ⚠️ Nota de leitura: o arquivo de migração chama-se `20260904010000_manutencao_pc_capacidade.sql` porque a sessão atravessou a meia-noite **em UTC** — as datas dos documentos deste projeto são de São Paulo, o fuso de `fn_hoje()` (card 2.3 c), e o card é de 03/09/2026.
- 03/09/2026 — **card 5.5,5 concluído: o board passou a poder ser executado sem alguém de plantão.** Nasceu fora do plano, de pedido de Irineu. `automacao/cadeia.ps1` abre **uma sessão headless por card** (`claude -p`), em sequência, lê a linha de veredito e para no primeiro sinal de que precisa de gente; `docs/cadeia-execucao.md` é a fonte. **A decisão que habilita tudo: `develop` é do CI; `main` é de Irineu** — o merge em `develop` deixou de exigir clique e passou a ser automático com o CI verde, porque o que o acordo de 01/09 protegia era o CI, e é o CI que passa a executar a regra em vez de um humano repetindo-a. A promoção para produção continua manual, sempre. **O driver é fino de propósito** e não escolhe card (isso é da skill — duplicar a lógica criaria duas fontes da verdade), e **não acredita na sessão**: `CARD_OK` é relato, e ele compara o SHA de `origin/develop` antes e depois, porque o pior desfecho de uma execução sem ninguém olhando é a sessão achar que mergeou, o board dizer `Concluído` e `develop` estar parado — o erro só apareceria três cards adiante, como conflito ilegível. **Três coisas quebravam a cadeia antes de ela existir, e as três foram medidas:** a negação por prefixo de `supabase db reset` (a forma **local** é o passo que o `testes.yml` roda para provar que a sequência de migrações sobe limpa num banco vazio — negar as duas juntas mataria a cadeia no primeiro card), `gh` fora do `allow`, e o `CLAUDE.md` mandando perguntar antes de mergear. ⚠️ **O guarda novo produziu o próprio defeito na estreia:** ele **bloqueou a abertura do PR deste card**, porque o corpo *descrevia* o comando perigoso e o texto viajava dentro do heredoc do `gh pr create` — *comando não é o texto que ele carrega*. Falha fechada, mas séria para a cadeia, já que as sessões deste projeto escrevem documentação densa sobre migração o tempo todo. Corrigido em três partes, **e a terceira existe por causa da segunda**: apagar o corpo do heredoc, ancorar o casamento no início do segmento, e tirar `VAR=valor` da frente — sem isso a âncora seria contornável, e o conserto de um falso-positivo teria aberto um **falso-negativo**. ✅ **Exercitado, não lido**: bateria versionada de **7 testes e 18 comandos**, os dois grupos (o que tem de passar e o que tem de bloquear), mais **contraprova em cópias sabotadas** — sem `semPrefixoEnv` o prefixo de ambiente passa; sem `semHeredoc` a prosa volta a bloquear. CI verde nos seis checks; PR #93. **Divergência registrada:** o plano 2+2 em paralelo da fase 05, escrito mais cedo no mesmo dia, perde a razão de existir — o paralelismo economizava o tempo de espera de Irineu, e sequencial elimina worktree, corrida de timestamp de migração e conflito em arquivo compartilhado. ⚠️ **Em aberto:** a bateria do guarda **não roda no CI**, e enquanto não rodar uma regex afrouxada passa despercebida no PR, que é exatamente o que ela existe para impedir.
- 03/09/2026 — **card 5.6 concluído: a grade semanal existe, e a semana é um parâmetro.** `supabase/migrations/20260904030000_grade_semanal.sql` traz `fn_grade_semana(p_segunda date)` e `v_bloco_vagas_semana` **escrita em cima dela**; `app/lib/turmas/` e `app/lib/telas/turmas/` trazem a tela 4 do wireframe (§7.1), que substitui as seis abas Segunda…Sábado da planilha. Só função e view — o portão do 4.0,5 confirma. **Por que as duas, e não só a view:** a lotação é de uma **data** (alocação vale toda semana, reposição vale só no dia), view não recebe parâmetro, e a tela navega semanas; a view fica sendo a semana corrente, que é o contrato do dashboard do card 5.9. **Três decisões no banco.** (1) **`p_segunda` é normalizada** com `date_trunc('week', …)`: a função responde pela semana de qualquer data que receba, e sem isso um dia de folga na conta mostraria a semana certa com as **datas** erradas — e a data só aparece no rótulo, então a ocupação sairia deslocada sem nada na tela dizendo. (2) **`security invoker`, ao contrário das duas funções do card 5.2**, e a diferença não é descuido: aquelas devolvem um número derivado, que não pode depender do que o leitor enxerga; esta devolve **linhas**, que devem — quem não tem `turmas.ler` não vê bloco nenhum, e quem decide isso é a RLS. (3) **Nenhuma conta de capacidade aqui**: as três parcelas são as três funções do 5.2 chamadas por linha, e não `greatest(cap − ocu, 0)` reescrito. **Duas decisões na tela**: a célula é uma **lista** de blocos (a `unique` é por sala, então duas salas cabem no mesmo dia e horário — mostrar só o primeiro perderia a outra turma em silêncio), e os blocos **inativos** ganharam lista própria, senão desativar seria porta de mão única. ✅ **Exercitado**: `flutter test` **322/322** (eram 285), `analyze --fatal-infos` e `format` limpos, portão de migrações verde. ⚠️ **Achado que o widget test encontrou antes do usuário**: `DropdownButtonFormField` tem um `assert` que exige que o valor inicial exista entre os itens, e `professoresProvider` só começa a carregar quando o formulário do bloco abre — abrir um bloco que já tem professor derrubava a tela com uma asserção do framework. Fechado com duas barreiras: o formulário **espera** as listas e id não resolvido degrada para "não escolhido". ⚠️ **A suíte do banco não foi exercitada nesta sessão** (o CLI do Supabase não está no PATH e a sessão não pode invocá-lo por `npx`): o portão é o job `banco` do CI, com o arquivo novo `supabase/tests/095_views_paridade.sql` (27 asserções). ⚠️ Ajuste registrado para o card **5.7**: desativar um bloco **não** desaloca ninguém — os alunos seguem com `bloco_aluno.ativo`, e a rotina do 5.5 não os vê como sem turma; hoje a tela avisa, e quem fecha é a tela de alocação.

- 03/09/2026 — **card 5.7 concluído: a turma passou a ter gente dentro, e desativar um bloco deixou de ser silêncio.** `supabase/migrations/20260904060000_alunos_do_bloco.sql` traz `v_bloco_alunos`, `fn_bloco_alunos(bloco, data)` e a substituição de `rt_pendencias_diaria`; `app/lib/telas/turmas/painel_bloco.dart` + `formularios_alocacao.dart` trazem a tela 4b do wireframe (§7.2), e `app/lib/telas/alunos/aba_turmas.dart` a aba Turmas da ficha (§6.4), mais a coluna Turmas e o ⚠ na lista (§6.1). Só view e função no banco — portão do 4.0,5 verde. **A divisão em duas peças é a decisão que estrutura o card, e é o card 5.6 invertido:** a lista de alunos de um bloco é de uma **data** (alocação vale toda semana, reposição vale só no dia) e view não recebe parâmetro, então `v_bloco_alunos` ficou com a metade **permanente** — uma linha por alocação ativa, com `bloco_ativo` como **coluna** — e `fn_bloco_alunos` é a lista da tela, escrita **em cima da view**. **`bloco_ativo` como coluna e não como filtro é o que fecha o ajuste do card 5.6**: alocação em bloco desativado deixa de contar como turma para `rt_pendencias_diaria` (as duas contagens passaram a ler a view) e para o ⚠ da lista, mas **continua visível na ficha**, que é a única tela de onde alguém a desfaz — filtrar dentro da view resolveria a pendência e criaria outra, com o aluno marcado "sem turma" e a aba Turmas vazia, sem nada a remover. Mais quatro decisões: **`fn_bloco_alunos` exige `turmas.ler` e `alunos.ler` explicitamente** (sem elas a RLS devolveria uma turma de dez como vazia, o precedente de `fn_rep_situacao`); **a coluna Turmas da lista só é renderizada para quem tem `turmas.ler`**, porque a rota exige só `alunos.ler + materiais.ler` e um perfil sem ela veria **todo mundo** com ⚠ falso; **a ocupação do painel sai da lista e não da célula**, senão erraria por um logo depois de adicionar alguém; e **a busca de aluno é lista com seleção, não `DropdownButtonFormField`** — com o filtro mudando a cada tecla o valor selecionado sairia dos itens e o `assert` do widget derrubaria a tela, que é exatamente a armadilha que o card 5.6 mediu. Nasceu `BadgeTipo` (§5.1/§2.4, contorno) com o teste que assere o par preenchido × contorno lado a lado. ✅ **Exercitado**: `flutter test` **365/365** (eram 322), `analyze --fatal-infos` e `format` limpos, portão de migrações 18/18 e varredor verde nas dezesseis. ⚠️ **A suíte do banco NÃO foi exercitada localmente** — Docker e `npx` estão fora do `allow` da cadeia (situação simétrica à do card 5.6): o portão foi o job `banco` do CI, com `supabase/tests/043_bloco_alunos.sql` novo (36 asserções). **E o CI cobrou uma:** a primeira versão da migração reescreveu `rt_pendencias_diaria` a partir do corpo do card **5.5** em vez do do **5.4**, reintroduzindo a seção de `BLOCO_ACIMA_CAPACIDADE` que o 5.4 tinha removido — `create or replace` desfaz uma decisão sem que nada no diff pareça errado, porque o texto de volta é código legítimo, só de outra época. Quem pegou foi a asserção que o próprio 5.4 deixou no teste 090; a lição — **substituir função inteira exige partir da ÚLTIMA definição aplicada** — ficou escrita na migração e virou duas asserções novas no 043, ao lado da substituição que a provocou. É a mesma família do defeito que o portão do card 4.0,5 teve em 02/09/2026. ⚠️ Divergências registradas em `docs/views-leitura.md` §12.1: `v_bloco_alunos` virou duas coisas, e `v_aluno_lista` continua sem existir — agora com motivo medido.

- 04/09/2026 — **card 5.8 concluído: o sistema passou a ter onde dizer o que está errado, e a virada REP ganhou quem a execute.** A tela 11 do wireframe (§14) em `app/lib/pendencias/` e `app/lib/telas/pendencias/`, mais o contador do menu em `shell_im360.dart`. **Sem migração**: `v_pendencias_abertas`, `fn_pendencia_resolver_id` e as duas funções da virada já existiam desde os cards 5.5 e 5.3 — este card é o **chamador** que faltava, e a família de falha que o 4.7,7 catalogou (função entregue, testada e invisível vale zero). **Cinco decisões.** (1) **A central é fila de trabalho, não relatório** (card 2.6 decisão 3): cada tipo tem a ação primária do §14.3, e ela **só é renderizada quando há para onde ir** — referência que a RLS ocultou e tela de destino que o perfil não abre tiram o botão, porque um botão que leva a "Sem acesso" ensina a não clicar nos outros. (2) **`REP_VIRADA` é executada daqui**, que é o motivo de a virada ser *sugerida* e não automática (card 2.5): escolher o bloco é justamente o que o `pg_cron` não podia fazer. O seletor **oferece o bloco em que o aluno já está mesmo lotado**, porque `fn_bloco_admitir` só disputa vaga quando a linha **entra** na conta (card 5.3, decisão 2) — filtrar por vaga esconderia a resposta mais provável, o horário que ele já frequenta. (3) **Marcar presença mora aqui**: `fn_reposicao_registrar` era a função sem chamador que o card 5.7 registrou, e é ela que quita a aula em aberto — o veredito recalculado volta em **diálogo**, não em snackbar (card 2.7 f), porque muda a próxima ação. (4) **Ignorar não promete silêncio** (card 5.5 c): `pendencia_aberta_uk` é único parcial, então a rotina reabre às 03:10 enquanto a condição valer, e o diálogo diz isso com todas as letras — o remédio de verdade é mudar a causa, não esconder o aviso. (5) **Severidade é ícone + texto, não badge**: o sistema já tem dois vocabulários de badge (preenchido = status do aluno, contorno = tipo na turma, card 1.9 §6), e um terceiro numa lista que mostra nome de aluno faria a forma deixar de dizer qual se está lendo. **Divergência registrada com o §14.1**, que desenha `[Ver] [✓]` na linha: as ações moram no painel — na linha, um `[✓]` deixaria resolver sem ler por quê, e o painel é o único lugar onde cabem os números do `REP_VIRADA` ("3 aulas em aberto, prazo até 12/10, cabem 2"), que são o que torna a sugestão acionável. Segunda divergência: as três ações de estoque apontam para **Materiais** e não para Compras, que é do card 6.8 e ainda não existe — os três tipos também só passam a ser abertos na fase 6, e quando a tela nascer a linha muda em um lugar só. ✅ `flutter test` **416/416** (eram 365, com `pendencias_test.dart` e `tela_pendencias_test.dart` novos), `analyze --fatal-infos` e `format` limpos, portão de migrações verde. ⚠️ Ajuste para o card **5.9**: o dashboard v1 é consumidor da mesma `v_pendencias_abertas` agregada por severidade (wireframe §5) — a agregação sai do que já está carregado, não de uma segunda consulta. ✅ **Cumprido em 04/09/2026 pelo card 5.9.**
- 04/09/2026 — **card 5.9 concluído: o dashboard existe, e a mesma view diz uma coisa diferente.** Tela 2 do wireframe (§5), **versão 1**, em `app/lib/dashboard/` e `app/lib/telas/dashboard/`; **sem migração nenhuma** — este card é **consumidor**, e `v_bloco_vagas_semana` é do 5.6. **Cinco decisões.** (1) **A célula mostra `vagas/capacidade`, e a da tela de Turmas mostra `alocados/capacidade`** — a mesma view lida ao contrário, porque o dashboard responde "onde ainda cabe alguém" e a grade responde "quem está onde". As duas nunca divergem em número, mas podem divergir em **quem lê**: por isso a legenda é parte da grade e o `Semantics` de cada célula diz "2 vagas livres de 10" por extenso. (2) **`vagas` é a SOMA das parcelas, nunca `capacidade − ocupação`** — as duas contas divergem no instante em que existe bloco acima da capacidade (`fn_vagas_livres` devolve 0 ali, card 5.2), e a diferença faria o excesso de uma turma **abater** a vaga real de outra: na fixture, 11 vagas viram 10. (3) **A grade é de UM método por vez**, com os cartões de método servindo de seletor — vaga de Inglês não serve a aluno de Interativo (`METODO_INCOMPATIVEL`, card 5.3), então somar dois métodos num cruzamento produz um número que ninguém pode usar e que faz a secretaria prometer vaga inexistente. **Divergência registrada** com o §5, que desenha uma grade só para "Interativo e Inglês". (4) **Sem navegação de semanas** (segunda divergência com o §5, que desenha `[semana ◄ atual ►]`): o dashboard é o retrato de hoje, a semana é a que o banco fixa com `fn_hoje()`, e um segundo navegador daria duas telas quase idênticas paradas em semanas diferentes — com a de baixo dizendo "semana corrente". **A semana rotulada sai de `data_referencia`**, e não do relógio do aparelho. (5) **As pendências abertas por severidade entram aqui**, contra a Nota do card, que as mandava para a fase 8: é o ajuste que o 5.8 deixou escrito, custa **zero consulta** (o shell já carrega `pendenciasProvider` para o contador do menu) e sem elas `pendencias.ler` ficaria no conjunto mínimo da rota **sem consumidor**. ⚠️ **Achado de framework, medido:** o **Riverpod 3 repete sozinho o provider que falhou**, então o estado passa por `AsyncError` e **volta a `AsyncLoading`** guardando o erro — casar pela classe faz a mensagem piscar e a região terminar em "Carregando…" para sempre. A região pergunta `hasError`, e o teste ficou **vermelho antes** da correção. Fecha também o **bloqueante nº 1** de `docs/permissoes-matriz.md` §7 para `v_bloco_vagas_semana`: `materiais.ler` entrou na tabela de §11 de `docs/views-leitura.md` e virou **asserção** no teste `095` (perfil `SEM_MATERI` vê a grade vazia e a recebe inteira de volta com a permissão) — as outras quatro views seguem com os cards 7.4 e 8.7. ✅ `flutter test` **445/445** (eram 416), `analyze --fatal-infos` e `format` limpos, portão de migrações verde, e as **duas asserções centrais vistas vermelhas** por sabotagem antes de aceitas (a leitura da célula e a soma das vagas). ⚠️ A suíte do banco **não rodou localmente** (Docker e `npx` fora do `allow` da cadeia) — situação idêntica à dos cards 5.6 e 5.7; o portão é o CI.
- 04/09/2026 — **card 5.10 concluído, encerrando a Fase 05: terceira rodada da estimativa, e o desenvolvimento deixou de ser o que decide a data.** Números na subpágina Notion **Estimativa de entrega** do card 3.13, que segue sendo o **único** lugar onde a data mora. **Go-live P50 02/10/2026, P80 19/10/2026** (eram 07/10 e 28/10); lojas P50 15/10, P80 23/11. Banda de 21 → **17 dias**, e a folga dentro de outubro passou de 24/3 para **29/12 dias**. Medidos **256 pontos em 7 dias** (29/08 a 04/09) em 56 cards; restam **155 pontos em 33 cards** até o go-live (128 de esforço + 27 de latência). **Quatro coisas que esta rodada registra.** (1) **O método não mudou, e é isso que ela tem de melhor a dizer:** foi a primeira rodada apenas *executada*, e ela **reproduziu exatamente** a série da anterior — os pontos por dia de 29/08 a 02/09 saíram 5, 11, 31, 75, 54, idênticos aos publicados em 03/09. Método que devolve números diferentes para os mesmos dias não mede nada. (2) ⚠️ **A latência deixou de ser só o gargalo e virou a maior parte da data:** no P50 são **18,6 dias corridos de latência contra 9,6 de esforço — 66%**. O teste que torna isso concreto: **com velocidade infinita o P50 seria 23/09 e o P80, 05/10** — todo o desenvolvimento que resta (fases 06 a 09 inteiras) vale 9,6 dias. Mais que dobrar a capacidade semanal compra **5 dias** (eram 9 na rodada anterior). (3) **O primeiro bloco de latência está sendo medido agora, e o relógio está ligado:** o marco 4.8 abriu em 03/09 com bloco declarado de 4 dias e continua aberto — 2 de 4 consumidos, sem aceite, à espera das duas preparações de Irineu em homologação (pendência 9.14) e da sessão com a secretaria. ✅ O que a mesma medição **já confirmou** vale mais: a latência do 4.8 correu **em paralelo** com o desenvolvimento (a cadeia entregou 5.6 a 5.9 enquanto o marco esperava), o que é exatamente a premissa dos 60% de sobreposição do P50 — o que segue sem resposta é a **duração** do bloco, não a sobreposição. (4) **A taxa de descoberta de escopo se confirmou** (4,2 → 4,6 pts/dia-pleno, em janelas diferentes) e por isso deixa de ser conserto e vira parâmetro do modelo; cards de fase 11 ficam **fora** dela, porque a fase 11 nunca entrou em estimativa. **Passo 2 com cinco redimensionamentos e saldo +2**: `6.1` `G`→`GG` e `7.1` `M`→`G` (escopo transferido pelas pendências 9.11 e 9.17), `6.3` `G`→`GG` (é o único card que constrói a suíte de concorrência, artefato de natureza inédita), `6.7` e `8.7` `GG`→`G` (telas que se acrescentam a páginas que **já existem** — Materiais do 4.4 e Dashboard do 5.9). ⚠️ O viés do §6(d) do método continua de pé e está escrito: as três subidas vêm de documento, as duas descidas de analogia, que é mais gentil; a contraprova disponível é fraca de propósito — a fase 05 inteira saiu no tamanho atribuído. **P80 mantido em P50 ÷ 1,5**, com o motivo registrado: a fase 05 não encostou em nada do que o divisor cobre (fase 09 e fase 10).

## Decisões-chave (resumo — detalhe na página Decisões vigentes)

- Nome **Gestão IM360**; domínio `gestaoim360.com`; app id `com.gestaoim360.app` (cadastro nas lojas pendente — avisar Irineu na fase de Publicação). Identidade visual definida internamente em 31/08/2026 (card 1.9), sem depender do dono do produto.
- Ambientes: Supabase **dev** `ncdfolxdupbbfvtydngx` e **prod** `aqfuawrygxsiopyppjza` (sa-east-1). Migrações **somente via CI/CD** (`develop` → dev; `main` → prod).
- Entrega sem estoque: não bloqueia — entrega a próxima apostila da trilha com estoque, reordenando a trilha (registrado no histórico); se **nenhuma** tiver estoque, bloqueia e gera pendência de compra.
- Parâmetros iniciais: projeção 60 dias; alerta STANDBY 30 dias.
- REP híbrido: eventos pontuais com data enquanto der para repor tudo no prazo; senão o aluno vira REP contínuo na alocação. **Critério objetivo fechado em 01/09/2026 (card 2.5):** o aluno é sugerido para contínuo quando o débito de aulas em aberto não cabe mais na capacidade semanal de reposição até o prazo de 30 dias da aula mais antiga, ou quando falta duas vezes à própria reposição. A virada é sempre sugerida, nunca automática.
- Perfis direção/pedagógico/secretaria/monitor com matriz de permissões configurável; `tem_permissao()` + RLS.
- Sentry desde a Fase 0 (free tier), além dos logs do Supabase.
- Credenciais dos PCs (card 2.9, 01/09/2026): segredo cifrado no Supabase Vault vinculado ao PC, lido só por `fn_pc_credencial_ler` com `salas.acessar_credencial` (direção e monitor) e log de acesso obrigatório. Nunca em texto puro, nunca migradas da planilha.

- 02/09/2026 — **card 3.9 concluído**, encerrando o pipeline: três workflows em `.github/workflows/`, documentados em `docs/ci-cd.md`. `testes.yml` (jobs `banco` e `app`) roda em **todo PR**, em todo push em `develop`/`main` e como workflow reutilizável; `db-migrations.yml` e o novo `deploy-web.yml` o chamam com `needs:` — **a suíte deixa de existir sem reprovar nada**, que era o primeiro dos dois bloqueantes que o card 2.8 apontava para cá. O segundo eram as **versões fixas**: CLI do Supabase **2.116.0**, Flutter **3.47.2**, Wrangler **4.128.0**. O `deploy-web` fecha o que o card 3.8 deixou (o Pages não tem Flutter no ambiente de build): `develop` → `gestao-im360-homolog`, `main` → `gestao-im360` com o *required reviewer* do environment `prod`, de modo que **produção passa a ter os mesmos dois portões humanos do banco**, agora também para o app. Cinco defeitos do YAML do Apêndice B do card 2.8 apareceram ao executar, e o que vale guardar é o terceiro: **chave de `--exclude` desconhecida não dá erro** — `supabase start -x inbucket` (nome válido no CLI 1.x, hoje `mailpit`) sai com **0**, sem aviso, e simplesmente não exclui nada; a exclusão escrita errada deixa de acontecer em silêncio, mesma família das falhas caladas que este projeto já catalogou em RLS, em view e em Redirect URL. O quarto era vermelho garantido no primeiro dia: o laço da suíte de concorrência, com o diretório vazio, passa o próprio curinga como nome de arquivo e sai com **127**. Decidido também o que a nota do card pedia: **o CI é o portão** da suíte de concorrência, e rodar na máquina é opcional — a dependência do Docker não é dela, é do job `banco` inteiro. ✅ **Verificado com o ferramental exato dos workflows, não lido**: `supabase test db` **115/115 `Result: PASS`** depois de `db reset` do zero; `dart format` 36 arquivos **0 alterados**, `analyze --fatal-infos` sem nada, `flutter test` **98 verdes**; e o `flutter build web --release --no-web-resources-cdn` com as quatro conferências do deploy passando, mais a contraprova de que o bundle de homologação **não** contém a URL de produção. ⚠️ Falta só o que é de Irineu: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` e `SUPABASE_ANON_KEY` em cada environment.

## Próximos passos (Fase 0)

1. ✅ **A configuração do CI está completa** (02/09/2026): os três secrets do Supabase, o *required reviewer* do environment `prod` e — criados por Irineu neste dia — `CLOUDFLARE_API_TOKEN` e `CLOUDFLARE_ACCOUNT_ID` no repositório mais `SUPABASE_ANON_KEY` em **cada** environment. Lista em `docs/ci-cd.md` §10. **Exercitado nos dois sentidos**: sem os segredos o `deploy-web` falhou no passo de conferência nomeando o que faltava; com eles ficou verde e `homolog.gestaoim360.com` passou a ser servido por um bundle **construído pelo CI**, o primeiro deploy automatizado do projeto. O que resta é sempre humano: o `publicar (PRODUÇÃO)` fica em `waiting` no environment `prod` até o *Approve and deploy*.
2. ~~Primeira migração de schema~~ e ~~funções de acesso + RLS~~ — **feitas nos cards 3.3 e 3.4**. As sete tabelas de organização e acesso estão com política, e `fn_hoje`, `fn_unidade_atual`, `tem_permissao`, `fn_param_int`/`fn_param_txt` e `fn_exige_permissao` existem. O que continua pendente e nasce nos cards seguintes: os `default public.fn_hoje()` no lugar de `current_date` nas colunas dos cards 4.2/5.1/7.1 (card 2.3, §10) e, no card 4.3, extensão `supabase_vault` habilitada com conferência de que o schema `vault` não está exposto no PostgREST (card 2.9, §11). As nove tabelas que fogem do padrão de quatro políticas (`permissoes-matriz.md` §4) são todas de fases posteriores — nenhuma existe ainda.
3. ✅ **Os dois ambientes estão no ar** (02/09/2026): **`app.gestaoim360.com`** (Supabase prod) e **`homolog.gestaoim360.com`** (Supabase dev), publicados no Cloudflare Pages por *direct upload* — o build fica com o card 3.9. Domínio `gestaoim360.com` registrado em 01/09/2026 e na conta Cloudflare; os dois *custom domains* anexados. Painel do Supabase preenchido nos dois projetos (Site URL, Redirect URLs `<url>/**`) e **SMTP próprio no ar**: Resend com `gestaoim360.com` verificado (DKIM + SPF em `send.`, região sa-east-1), remetente `nao-responda@gestaoim360.com`. **Conferido de ponta a ponta**, não lido: `/`, `/alunos` e `/redefinir-senha` respondendo 200 nos dois, `_headers` aplicado (CSP, cache, X-Frame, Referrer), cada bundle apontando só para o seu Supabase, e o fluxo de recuperação exercitado com e-mail real nos dois ambientes — entregue pelo Resend e com o `redirect_to` **honrado**, caindo na tela de nova senha em vez da Site URL. ⚠️ Teto do Resend free: 100 e-mails/dia. **O lado do banco já está pronto (card 3.5):** Auth por e-mail/senha configurado, cadastro público fechado, espelho `auth.users` → `usuario` no ar e os oito usuários da fixture logando no stack local. O que falta antes de o 3.7 ter em quem logar no **dev** é o seed do card 3.6 — convite cria o espelho, mas sem `usuario_perfil` a pessoa não pode nada. Site URL e SMTP dos projetos remotos: ver `docs/acesso-autenticacao.md` §7.
4. ✅ **Worker do Cloudflare no ar** (card 3.10, 02/09/2026: `gestao-im360-vigia`, cron diário 06:00) e ✅ **backup semanal escrito** (card 3.11: domingo 06:30, dump → ensaio de restauração → R2, `docs/backup-restauracao.md`). ✅ O backup está **no ar e exercitado** desde 02/09/2026 (bucket R2 e secrets criados por Irineu; primeira cópia em `producao/2026-09-02/`). ✅ **Sentry no Flutter ligado** (card 3.12, 02/09/2026), com o host de ingestão no `connect-src` no mesmo commit e asserção dos dois lados; e o vigia passou a conferir a idade do backup no R2, fechando o modo de falha silencioso do card 3.11. ⚠️ Falta só Irineu cadastrar o secret `SENTRY_DSN` — até lá o app publica sem observabilidade e o `deploy-web` avisa.
5. **A suíte roda com o ferramental de verdade.** Em 01/09/2026 (cards 3.4.5, 3.5 e 3.6) `supabase start` + `supabase test db` rodaram ponta a ponta em sessões do Claude Code na nuvem (o daemon do Docker não sobe sozinho: `dockerd &` antes do `supabase start`) — pgTAP 1.3.2, imagem `supabase/pg_prove:3.36`, **73 asserções, 73 verdes** — e **115/115 no card 3.6**, com quatro migrações, com as duas migrações e o `seed.sql` aplicando limpo. Isso substitui o resultado anterior (44/44 num container `postgres:17-alpine` com stub e shim de pgTAP, card 3.4) e **fecha a dúvida que ele deixou**: o pgTAP real aceita as mesmas chamadas. Consequências: o Docker e o CLI do Supabase existem no ambiente das sessões na nuvem, então a suíte tem onde rodar mesmo que a máquina de Irineu não os tenha; e o card **3.9** deixa de precisar decidir *se* dá para rodar — decide o **portão**. ✅ **Fechado em 02/09/2026 pelo card 3.9**: `testes.yml` em todo PR e chamado com `needs:` antes do `db push` e do deploy, `setup-cli` em 2.116.0, Flutter em 3.47.2 e `major_version = 17` já no `config.toml`. **O CI é o portão; rodar na máquina é opcional** — a dependência do Docker é do job `banco` inteiro, não da suíte de concorrência. Continua pendente a suíte de concorrência de duas sessões (cards 5.3 e 6.3), cujo diretório e contrato já existem em `supabase/tests_concorrencia/`. ⚠️ `.claude/settings.json` nega `psql`, `supabase db push` e `supabase db reset` — a via correta é `supabase start` + `supabase test db`, que não toca banco remoto nenhum.

6. Desabilitar "Automatically expose new tables" nos dois projetos Supabase (Settings → API) quando o schema começar a existir.

O sequenciamento oficial dessas tarefas está no board do Notion — use a skill `proxima-tarefa`.

## Convenções

Ver "Regras inegociáveis" no `CLAUDE.md`. Em resumo: regras de negócio no banco; RLS em tudo; português snake_case; `unidade_id` + auditoria em toda tabela; estoque imutável com estorno; credenciais nunca em texto puro.
